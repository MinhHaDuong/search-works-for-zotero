{
  "version": "2.3.0",
  "change": "State transitions journalled to Zotero.debug() and a volatile 2000-record in-session ring",
  "retires": "2.1.5 — sdt-sitter-errors.jsonl and its IOUtils append are removed; no code path writes that file",
  "path": "none; nothing is written to disk by this version",
  "pref": "extensions.sdt-pack-sitter.debug gates the trace level only; error and state always reach Zotero.debug()",
  "privacy": "each record carries an explicit field whitelist, and error text is scrubbed of paths and filenames before it is emitted",
  "workState": "not authoritative; the ring dies with the session and the cache records remain separate"
}
