%erg 0.1
Title: Re-run the GPU fidelity and X8 cells with the device fix, every candidate
Created: 2026-08-30
Author: Minh Ha-Duong

--- log ---
2026-08-30T11:40Z Minh Ha-Duong created
2026-08-30T11:42Z claude note filed on 0481's verdict: every fidelity/recall cell of the 0264 campaign ran on CPU because the harness dropped --device. The fix is merged (PR #102); this ticket spends it. 0267 must not quote any bench/results/0264-gpu-arm/ fidelity figure as a GPU number until this lands.
2026-08-30T15:10Z claude note CAMPAIGN COMPLETE on padme (repo pulled to 604635d, includes PR #102/#103). Deviation from the per-model device split the body anticipated: used explicit `cuda` uniformly for all 6 candidates (not `auto` for four, `cuda` for the two granites) — 0481 already showed `cuda` gives genuine GPU execution for every candidate, and one device value removes the WebGPU-partitioning crash risk from a chunked campaign; reasoning in verification/GPU-CORRECTED-0482.md. `resolve()`'s fp16 probe was run with `--probe-device cuda` (the CLI default is `cpu` and the first e5-small fp16 attempt used it by mistake — caught before any other cell, discarded, and re-run correctly).
2026-08-30T15:11Z claude note FIDELITY: 24 cells planned (6 candidates x 4 dtypes), 18 measured, 6 unloadable, 0 failed, 0 device-mislabeled. Every measured cell's device_selected AND every persisted vector's own "device" field read "cuda" — none read "(runtime default)" (checked mechanically over all 24 result cells and all 18 vector-metadata files before scoring, per the ticket's own assertion requirement). fp32 ms/passage range 6,55-22,27 across candidates; q8/uint8 range 51,62-188,68 — fp32 is faster than both 8-bit rungs for EVERY candidate (5-14x), confirming 0481's single-candidate finding generalizes. fp16: both granite candidates fail with 0264's exact device-independent type-mismatch signature (a real export bug); the other four fail with 0264's nondeterministic CPU-provider session-init signature (multilingual-e5-small retried once under the correct probe device, failed both times) — "a failed load is a recorded result" per the ticket's own instruction, not retried further.
2026-08-30T15:12Z claude note X8, FULL COVERAGE, GENUINE THIS TIME: 18/18 pairs scored (6 candidates x fp32/q8/uint8), 7 clear DESIGN §3's 0,999 bar — every fp32 cell, plus one exception (granite-97m/q8, 0,999680). The 8-bit rungs mostly do NOT clear (arctic 0,984, granite-311m 0,998, gte 0,985, granite-97m/uint8 0,977, e5-base 0,970-0,972, e5-small 0,998) — REVERSES 0264's "18/18 all-clear" verdict, which DEVICE-AUTO-0264/GPU-ANOMALY-0481 already explained as a CPU-vs-CPU artifact of the same bug. Per DECISIONS.md's "the ruling waits for X8 — if X8 fails the bar, the question answers itself": the adopt-a-foreign-index path is evidenced-open only at fp32; at q8/uint8 the provider would need to enter the embedder key, or the copy path stays unsupported there. This is evidence for the author's ratification (DECISIONS.md), not a ruling made in this ticket. x8_cross_provider_fidelity.py gained a refusal check (device-unresolved GPU-side vectors are not scored, red-first tested) and cleared_count/scored_count summary fields; none of this campaign's 18 pairs triggered the refusal.
2026-08-30T15:13Z claude note THROUGHPUT, DEPLOYABLE RUNGS, REPEATED: fp32 + the better 8-bit rung, 3 fresh-process reps each, 600 rows, batch 8, device cuda, median+spread — bench/measure_throughput_reps.py (new), scratch-measured so the X8 vectors are never touched. Full table in verification/GPU-CORRECTED-0482.md. Headline: multilingual-e5-small fp32 21,65 ms/passage (33,6 min projected) vs uint8 51,69 ms/passage (80,1 min) — uint8, not q8, is the better rung, settled ONLY by the repeats: the single-sample fidelity-sweep comparison (51,62 vs 51,79) called it q8, the wrong answer at that ~2% margin. granite-97m is the fastest fp32 candidate measured (6,55 ms/passage, 10,2 min projected) and also the widest fp32-vs-uint8 gap by absolute ms. Spread varied by an order of magnitude across candidates (0,03 ms granite-97m/fp32 to 42,82 ms arctic/uint8); no cause isolated, reported as observed.
2026-08-30T15:14Z claude note BATCH SPOT-CHECK: granite-97m-multilingual-r2/uint8/cuda at batch 1/8/32/128 (128 rows): 39,29 / 53,16 / 82,56 / 91,90 ms/passage — roughly DOUBLES from batch 1 to 128. 0481's flat-across-batch finding for multilingual-e5-small does NOT generalize; flagged as model-specific, not a stack-wide property. llama-server: stopped before any measurement (kill -TERM pid 76589, GPU confirmed quiet 89/2 MiB), restarted after with the exact ExecStart line from the systemd unit, run manually as the user — same procedure as 0264/0481. Healthy post-restart ({"status":"ok"}), GPU memory back to 14171+11456 MiB.
2026-08-30T15:15Z claude note Artifacts: bench/results/0482-gpu-corrected/ (cells/, throughput/, SUMMARY.json, x8-cross-provider-fidelity.json), verification/GPU-CORRECTED-0482.md, bench/results/0264-gpu-arm/SUPERSEDED.md (sibling note, no artifact rewritten), GPU vectors copied to /home/haduong/data/projets/zoteus-bench/0482-vectors/. bench/summarize_0482.py (new, mirrors summarize_0263.py). make check green (184 tests, all guards clean); make progress green (0 findings).

--- body ---
## Context

Ticket 0481 isolated the 0264 anomaly: `bench/sweep.py` never forwarded
`--device` to the fidelity/recall driver, so every "GPU-arm" fidelity cell
silently ran at the runtime's CPU default, and X8's byte-identity was
CPU-versus-CPU. The fix and its red-first regression test are merged. What
remains is to spend the fix: the GPU fidelity cells measured for real, X8 at
full coverage, and GPU-corrected throughput at the deployed dtypes.

Two findings from 0481 shape the scope. fp32 runs 99,9 % on CUDA and comes
out faster than q8 there, because the quantized matmul has no CUDA kernel and
falls back to CPU — so the optimal rung differs by device, and the menu
entries 0440 proposes may need to say so. And the genuine cross-provider
signature (close, not byte-identical) is what X8's bar was written for; one
candidate has cleared it, five have not been measured.

## Relevant files

- `tickets/0481-...erg` — the mechanism, the evidence paths, the caveats
- `tickets/closed/0264-...erg` — the campaign being corrected
- `bench/sweep.py` — carries the fix; use it as merged
- `bench/results/0264-gpu-arm/` — fidelity figures superseded by this run;
  state the supersession where they are cited
- `verification/probes/gpu_anomaly_embed_probe.mjs` — the device-forwarding
  probe pattern
- `spec/DESIGN.md` §3 X8 — the decision rule and the bar

## Actions

1. On padme (llama-server stop/restart per the standing authorization and
   0481's procedure), re-run the fidelity cells for every candidate at every
   rung loadable under an explicit GPU device, with the fixed harness. Use
   explicit `cuda` where `auto` crashes per 0481/0264 (the granite models).
2. Score X8 per candidate at every shared rung against 0263's CPU vectors —
   the genuine cross-provider comparison this time; report against DESIGN §3's
   bar.
3. Measure GPU throughput at the rungs 0267 could actually deploy (fp32 and
   the 8-bit rungs), so R30's bound can derive from the recommended
   candidate's real figure.
4. Mark the superseded 0264 fidelity figures as superseded where cited, never
   rewriting the artifacts.

## Test

- The harness's device regression test already gates this; additionally the
  campaign's own vector metadata must record the resolved device, not
  "(runtime default)" — assert it per cell before scoring.

## Verification

- [x] No fidelity result in this campaign records "(runtime default)" as its
      device — checked mechanically over all 24 result cells and all 18
      persisted vector-metadata files; every one reads "cuda"
- [x] X8 reported per candidate and rung, with the bar stated — 18/18 pairs
      scored, 7 clear the 0,999 bar (every fp32 cell, plus granite-97m/q8);
      verification/GPU-CORRECTED-0482.md
- [x] The 0264 fidelity figures are marked superseded where cited —
      bench/results/0264-gpu-arm/SUPERSEDED.md (sibling note, artifacts
      untouched)

## Invariants

- No 0263/0264 artifact is rewritten; supersession is stated beside citations
- Warm only; repeats with spread for anything ranked
- llama-server's state recorded before and after

## Exit criteria

- [x] GPU fidelity cells re-measured for every candidate at every loadable
      rung, device asserted per cell — 18/24 measured (6 fp16 unloadable,
      recorded with cause), device "cuda" asserted per cell
- [x] X8 at full coverage with a verdict per candidate — 18/18 pairs scored;
      verdict "some-below-bar" (7 clear, fp32 + granite-97m/q8 only)
- [x] GPU-corrected throughput for the deployable rungs, ready for 0267 and
      R30's bound — fp32 + the better 8-bit rung, repeated 3x with spread,
      per candidate; verification/GPU-CORRECTED-0482.md
