%erg 0.1
Title: Follow up SDT sitter panel accessibility and wording findings
Created: 2026-09-05
Author: codex

--- log ---
2026-09-05T21:04Z codex created

2026-09-06T12:08Z claude note 0693 reorganized the status dialog into three disclosure layers, so the keyboard/focus/Escape/reduced-motion items here must be verified against THAT layout, not the pre-0693 one. verification/probes/sdt-sitter-harness/bootstrap.js now asserts the layer structure, nesting, closed-by-default, the bound checkbox label and focus on each summary, and records the reduced-motion reading and the toolbar accessible name; running that probe in a real Zotero window is what remains.
2026-09-08T16:00Z claude note The accessibility item the harness recorded as unbuilt is built: aria-label pins the toolbar's accessible name to the coverage figure, and the spinner, census pulse and completion blink stop under prefers-reduced-motion. Two scenarios in tests/sdt_sitter_bootstrap.mjs, each with the animated arm as its positive control; the sitter harness probe now asserts the name in a real window instead of recording it. The ticket stays open on three items. (1) Transition-only announcements through a status region: nothing in the dialog is a live region today and the status line is rewritten at 10 Hz, so marking it live would announce every tick. (2) The keyboard/focus/Escape/focus-return verification 0693's log entry assigns here — the sitter harness probe asserts focus on each summary, and opening, closing and focus return still need synthesized key events in a real window. (3) Distinguishing complete coverage from a finished pass with exceptions, still marked Follow-up in verification/SDT-SITTER-UI-PANEL.md. The other wording items (human-readable phases, title/filename, diagnostics below progress) were built by 0691/0692/0693.
2026-09-08T17:04Z claude note Ticket-status sweep after seven sitter PRs merged by direct API call, which skipped every ticket close. This one is CONFIRMED OPEN and is not merely unfinished bookkeeping. Verified built on origin/main: the toolbar's accessible name (`aria-label` pinned to the coverage figure) and reduced motion on all three animations, each with its animated arm as a positive control — `tests/sdt_sitter_bootstrap.mjs` runs "the spinner does not rename the button, and reduced motion stops it spinning", "reduced motion stops the completion blink", "reduced motion stops the census pulse" and "a window that throws from matchMedia leaves the render loop running", all green. Still owed, unchanged from the 2026-09-08T16:00Z entry and now also carrying the item ticket 0693 handed over on closure: (1) transition-only announcements through a status region, blocked on the status line being rewritten at 10 Hz; (2) keyboard open/focus/close/Escape/focus-return in a real Zotero window, against 0693's CURRENT layout — the census-account rendering moved it again on 2026-09-08 — which needs synthesized key events no headless lane has; (3) distinguishing complete coverage from a finished pass with exceptions, still marked Follow-up in verification/SDT-SITTER-UI-PANEL.md. The harness probe verification/probes/sdt-sitter-harness/ carries the assertions; running it in a real window is the work.

--- body ---
Requested panel results are recorded in `verification/SDT-SITTER-UI-PANEL.md`.
Follow-up proposals, not an accessibility approval: stable accessible button
name and reduced-motion support; transition-only status announcements; verify
keyboard opening, focus, closing/Escape and focus return; human-readable states
and document title/filename; distinguish complete coverage from finished passes
with exceptions; move technical diagnostics below primary progress information.

Before implementation, verify remaining visual and assistive-technology claims
in a real window. The existing DOM/style smoke does not establish keyboard or
screen-reader behavior. The finish-estimate overrun finding was addressed in
0685; do not re-file it as unbuilt.
