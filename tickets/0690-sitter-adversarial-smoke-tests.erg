%erg 0.1
Title: Add adversarial scheduler-seam tests for the sitter (doable now)
Created: 2026-09-06
Author: claude
Blocked-by: github.com/MinhHaDuong/search-works-for-zotero#365

--- log ---
2026-09-06T00:00Z claude created
2026-09-06T00:00Z claude note Imagine phase (raid, model sonnet) ESCALATEd the original combined ticket: ~7 of 13 scenarios needed a Zotero-environment mock harness that does not exist anywhere in this repo's test tree, one scenario (clock jump backwards) asked for monotonic-time behavior that does not exist in the code -- a design decision, not a test -- and one row referenced an uncoded counter. Split per its recommendation: this ticket keeps only the scenarios testable today with the existing `host` mock pattern in `tests/sdt_sitter_scheduler.mjs`; the rest moved to 0695.

2026-09-06T08:46Z claude note Execute phase: 2 tests written, not 4. Row 2 (pack absent/corrupt after a resolving ensure) IS the existing test "native success without current persisted cache counts as failure" -- scheduler.js line 62 folds !ok and after.status !== current into one throw, so both rows drive the same branch. Row 4 (disable during active job) IS the existing test "disable during ensure leaves completion but no UI callbacks or next admission". Neither was re-added.
2026-09-06T08:47Z claude note Red control arms against the branch's first commit, subject "test(0690): cover the two scheduler seams no existing test reaches" (535abd9 when written, f671602 after the rebase onto origin/main -- prefer the subject, a hash on an unmerged branch is renumbered by every rebase), scheduler broken in the working tree one break at a time, each run reporting per-test verdicts instead of aborting on the first failure. (a) Drop the host.reportError call at scheduler.js:73 -- 9 of 10 PASS, only "native rejection mid-job is reported once and the next document is still admitted" FAILs. (b) Hoist host.blocked out of the per-candidate loop into one pre-loop check -- 9 of 10 PASS, only "resources crossing the threshold between admissions refuse the next, not the running job" FAILs, the existing constant-blocked test staying green. Each break isolates exactly its new test: the overlap criterion executed rather than asserted. A third, coarser break (deleting the per-candidate catch so the throw escapes to the outer catch) also reddens the new rejection test, and two existing ones with it, which is why the sharper breaks above are the ones recorded.
2026-09-06T08:58Z claude note Adherence pass caught a stale count: this ticket said "the existing seven tests" where origin/main has eight. The count was decorative -- the overlap claim it sat beside is proven by the control arms, not by the number -- so both occurrences now name the file instead of counting it, per the AGENTS.md convention that a duplicated number drifts.
2026-09-06T09:18Z claude note Review round 1 red team: 4 mutants survived all 10 green tests. The headline one is real and mine -- break -> continue on the resource-blocked branch survived the threshold test, because the fixture had 2 candidates, so after refusing the second there was nothing left to iterate and halting looked identical to skipping. Verified by replaying that mutant against that same first commit: 0 failures. The doc-comment claimed the test pinned the halt; it pinned only that the check is per-candidate. Fixed by widening the fixture to 3 candidates and making the dip transient (blocked on the second admission only, comfortable again on the third) -- with a persistent dip a third candidate is refused under both break and continue and the mutant still survives, so the width alone would not have done it. The other three (counts[status]-- in the catch, state.error, the finally clearing state.active/state.pending) and state.samples.push are now asserted inside the two existing tests rather than in new ones, per the ticket criterion that new tests must not duplicate. The matrix is committed as a re-runnable probe rather than a prose table -- verification/probes/sdt_sitter_scheduler_mutants.py, 8 mutants, 8 caught, exit 0; seven of the eight are caught only by the two new tests, and the eighth is the coarse catch-deletion that reddens existing ones too.
--- body ---
## Context

The author wants adversarial smoke testing of the plugin, not just the
happy-path DOM/style smoke that already exists (per ticket 0686's own
note: "the existing DOM/style smoke does not establish keyboard or
screen-reader behavior" -- the same gap applies to the scheduler's
failure handling).

`scheduler.js`'s `createSDTSitter(host)` already takes an injectable
`host.now()` for every timestamp, and `tests/sdt_sitter_scheduler.mjs`'s
fixture already injects a fake clock -- confirmed in code, not to-add.
The four scenarios below are testable today, in the same style as the
tests already in that file, with no new harness and no dependency
on ticket 0689's journal (assert against `host.reportError` calls and
scheduler state directly; do not invent a dependency on an unbuilt
journal for scenarios that don't need one).

## Scenarios

| Scenario | Fake | Expected |
|---|---|---|
| Worker rejects mid-job | `ensure()` rejects after 3 progress ticks | Suppressed by source/processor identity; next document admitted; `host.reportError` called once |
| Worker resolves, pack absent/corrupt | `inspect` finds nothing | Not counted as coverage; treated as a failure, not a silent success -- check this isn't a near-duplicate of the existing "native success without persisted cache counts as failure" test before writing it |
| Disk crosses threshold mid-job | resource reading goes from comfortable to below-threshold between admissions | Current job continues uninterrupted (admission-time check, not a running cap); the next admission is refused |
| Disable during active job | `stop()`/shutdown called while a submission is pending | No further `submit` after the call; the pending promise is not force-cancelled |

Worker-process death itself (kill the worker, not just reject a promise)
is out of scope -- that is ticket 0681's crash-recovery detection.

## Acceptance

- [x] Each row is one test, shown red once against a deliberately broken
      scheduler -- record the red commit hash in this ticket's log.
      Two rows, not four: the overlap check below retired the other two.
      Red arms taken one break per run against this branch's first
      commit, logged above, and re-runnable at any commit via
      verification/probes/sdt_sitter_scheduler_mutants.py.
- [x] Tests run under this repo's existing `make check` (no separate
      "integration tier" selector exists in this project's Makefile/
      conftest -- tag with the registered `integration` marker for
      classification only, it is not a gate).
      `tests/test_sdt_sitter.py` is the pytest wrapper that spawns node;
      both of its functions now carry the marker.
- [x] Each test is checked against every test already in
      `tests/sdt_sitter_scheduler.mjs` for overlap before being counted as
      new coverage. Two rows failed that check and were dropped; the two
      kept rows each have a break that reddens them and nothing else.
