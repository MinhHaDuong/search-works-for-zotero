%erg 0.1
Title: Scoped issue A: ledger, freshness, counters -- pause and serve-stale as the motivating defects
Created: 2026-08-26
Author: claude
Blocked-by: 0032

--- log ---
2026-08-26T16:52Z claude created

2026-08-26T17:11Z claude note Copilot round 4: train + harness-offer ordering mechanized as Blocked-by
2026-08-28T06:55Z codex note upstream #24 is direct third-party demand for the resume slice: a stopped local-API build restarts from zero. Use that existing thread for a testable resume contract (durable committed progress, version stamp not used as the work cursor, idempotent convergence); do not file a duplicate. The broader scoped issue A still carries the remaining ledger/freshness/counters, pause, serve-stale, custody and query-semantics work
2026-08-28T07:06Z codex note v1.8.0 code seam for the #24 comment: buildIncremental already makes partial work durable every 200 items or 10 s in metadata, 500 items or 60 s in full text, and reloads it queryably, but each new full build calls reset() and sets start=0. Suggest a separate persisted build checkpoint, not the full ledger. Performance contract: redo at most the last uncommitted batch; never re-embed committed passages; no full passage scan merely to resume
2026-08-28T07:08Z codex note resume contract posted on upstream #24: https://github.com/oscardvs/zoteus/issues/24#issuecomment-5449540874
2026-08-28T16:24Z Claude note blocker 0015 closed — Blocked-by removed.
2026-08-29T09:58Z Minh Ha-Duong note blocker 0014 closed — Blocked-by removed.
2026-08-29T14:07Z Minh Ha-Duong note blocker 0017 closed — Blocked-by removed.
2026-08-30T09:05Z Minh Ha-Duong note scope grows by ruling (spec/DECISIONS.md 2026-08-30, argued from X6's decoupling finding): the ledger design this issue carries upstream includes the content-presence probe and the cache-lost warning state (DESIGN.md 2.4 part iv) — served-not-evicted, counted, Reindex surfaced. The probe's cadence is pinned when this issue's machinery lands
2026-08-31T14:53Z Claude note blocker 0016 closed — Blocked-by removed.
2026-08-31T14:54Z claude note upstream advanced to v1.12.0 (b05ed69) 2026-08-31. Blocker 0016 is CLOSED — PR-3 merged as daf576b in v1.11.0 — so the wipe-guard half of the custody work is now upstream and this ticket no longer waits on it. Two of scoped issue A's slices have also been built by the maintainer since it was written: the resume contract (#24, v1.10.0) and now, in v1.11.0, a genuine index migration ladder plus vector salvage (#34), which is ledger machinery this ticket assumed absent. What remains here — freshness, counters, pause, serve-stale, custody and query semantics — must be re-derived against v1.12.0 before it is argued: the search layer moved +1738/-57 over ten files, three new, and index-manager.ts alone took +454. Do not re-file from the v1.10.0 reading
2026-09-02T06:33Z claude note THE TOOL SURFACE IS THIS ISSUE'S VISIBLE FACE, and it is negotiated, not ruled (author, 2026-09-02). zotero_index enumerated at v1.12.0 in DECISIONS.md's awaiting entry: status | update | build | refresh | stop, nothing per item, stop in memory. What this repo asks of the surface is a CONTRACT -- durable pause (R22), per-item repair, honest status (R17), no whole-library re-derivation without consent -- and the maintainer picks the verbs. Form per GOVERNANCE.md: design-sized, so an issue he builds himself, inside this one rather than a new filing. Re-derive against v1.12.0 first, as the 2026-08-31 note already says: update is now the cheap delta the chain calls sync, and #24's resume contract is shipped.
2026-09-02T08:01Z upstream #48 (2026-09-02, @Michael-Logies) is the second third-party carrier of this ticket's resume slice after #24: the metadata checkpoint he built after #24 records crawl progress, not embedding progress, so a 429 mid-pass leaves stored passages without vectors that nothing re-queues, and the only retry is a full re-embed. Draft comment with the embed-phase resume contract at verification/ISSUE-48-COMMENT-DRAFT.md, unposted; the seam is embeddings.ts:313 + index-manager.ts:1745-1753 at v1.12.0, the retry loop already in http.ts:81-87 is not on the embedder path. Do not open a competing issue
2026-09-02T10:14Z #48 comment posted 2026-09-02 after the author's read: https://github.com/oscardvs/zoteus/issues/48#issuecomment-5507982796 — verified on the page, no trailer. Carries the embed-phase resume contract as one query on the existing nullable passages.vector column, the transient/persistent retry split behind RateLimitedFetcher, token-budget sizing, and a five-step test on FakeEmbeddingProvider. Response pending; sunset from today
--- body ---
## Context
DESIGN.md §4 item 6-A, per the ratified re-form (DECISIONS.md 2026-08-26).
One #10-shaped issue: documented behaviors, reproductions, measurements, no
code to accept. Carries: the ledger/freshness/counters complex (DESIGN §2.1,
§2.4, §2.8); the PAUSE defect as motivation (verified: stop cancels one job,
auto_build default-on restarts on the next query — index-tool.ts:56-65,
semantic-search.ts:48-65; the R22 violation); the SERVE-STALE defect as
motivation (verified: dropStaleVectors -> clearVectors() at open,
index-manager.ts:286-308, plus the query-time dimension drop at :889-892 —
semantic coverage zeroed by an upgrade; dropStaleVectors was written on
purpose, so the issue names the alternative reading rather than treating it
as an oversight); the legacy-JSON custody paragraph (R15: a stale
search-index.json retains deleted items' text forever — the folded I-4); and
the D5 query-semantics work riding along or waiting for demand (the verified
repro: "general equilibrium" retrieves either-word today; interim item scope
per DESIGN.md §2.6). Absorbs the content of closed
tickets 0020, 0021, 0023.

Upstream [#24](https://github.com/oscardvs/zoteus/issues/24), filed by
@StianOby on 2026-08-27, now owns the user-visible resume slice: after a
stopped local-API build, the missing/invalid library version stamp sends the
next run back to zero. Do not open a competing issue. Offer #24 the behavioral
contract this design needs: already committed work survives stop and process
restart; the Zotero version stamp is a freshness signal, not the work cursor;
repeating an interrupted unit is safe; and resumed and uninterrupted builds
converge to the same index. The maintainer remains free to implement that
without this repo's full ledger design.

At v1.8.0 the narrow implementation seam is already visible:
`buildIncremental()` persists partial work (every 200 items or 10 seconds in
the metadata pass; every 500 items or 60 seconds in the full-text pass), and
the reloaded partial index is queryable, but a new full build unconditionally
calls `reset()` and initializes its page cursor at zero.
A small persisted build checkpoint beside the committed rows could carry the
phase and progress identity without overloading `libraryVersion`. The useful
performance bounds are: redo no more than the last uncommitted batch; never
re-embed a committed passage merely because the process restarted; and do not
scan every stored passage merely to find the resume point.

## Exit criteria
- [ ] issue drafted: behaviors + repros + measurements, no code to accept
- [x] #24 comment offers the resume contract; posted 2026-08-28, response pending
- [ ] filed after the PR train resolves (DESIGN §4 ordering)
- [ ] response recorded here; sunset rule applies
