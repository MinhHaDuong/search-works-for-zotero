%erg 0.1
Title: Library-derived droplist plus degeneracy fallback, upstream
Created: 2026-08-29
Author: claude

--- log ---
2026-08-29T10:06Z claude created
2026-08-29T10:10Z claude note the update-cadence question is ANSWERED by measurement, not left open — see "The cadence question, settled" in the body. FTS5 does not give the high-df terms for free: the vocabulary scan costs ~3,4 s warm and 7,5 s cold over 639 888 terms, so the droplist is derived state and needs a home, but it is 23 terms and 53 bytes and the index build already costs 263,7 s, which makes the build the obvious and only recompute point

2026-08-29T10:25Z claude note cross-granularity control on the droplist, measured 2026-08-29 against Zotero 10's own FTS5 index (fulltext.sqlite, 13 090 documents, item-level): at the same 30% threshold that yields 23 terms on our 477 512-passage index, the platform index yields 777 — 34x more, because document frequency at item granularity is a different quantity from document frequency at passage granularity. Its top terms are the bare digits (1 in 11 873 of 13 090 docs, 2 in 11 755, 10 in 11 217) since every document carries page numbers, years and references. Importing a droplist from the platform would drop every numeral from every query. This is independent evidence FOR this ticket's central choice: corpus-derived means derived from THIS corpus at THIS granularity, and the ticket already gets that right. No design change implied. Two further notes: the scan is not cheaper on the platform side either (670 680 terms in 4,0 s there against 639 888 in 3,4 s here), so the meta-row-plus-drift cadence stays necessary; and fts5vocab reads term and doc counts happily off a CONTENTLESS table, so vocabulary statistics are available from the platform index even though its text is not (see 0120). Worth a cheap check while here: whether our own 23 terms include numerals, and how the degeneracy fallback behaves on a numeric query such as 2019 or a DOI fragment — passage granularity likely keeps digits well under threshold, but it is cheaper to check than to learn from a user.
2026-08-30T16:28Z Codex note blocker 0090 closed — Blocked-by removed.
2026-08-31T20:20Z claude note anchors re-verified against upstream b05ed69 (v1.12.0) BEFORE implementing, because UPSTREAM still names v1.10.0 and v1.11.0/v1.12.0 rewrote the search layer under us (+1738/-57 over ten files, SYNC.md 2026-08-31). All three survive; two moved. tokenize.ts still opens on the 29-word literal at line 1 and applies it in tokenize() at line 150. sqlite-index.ts's keyword path is keywordSearch at line 1134, not ~466, and still opens with [...new Set(tokenize(q))]; the meta helpers, createSchema and the FTS5 declaration are still in that file. index-manager.ts's makeSnippet is at line 150 and still centres on the earliest matching token. bm25.ts is untouched at 2,6K. What the rewrite added under the same roof, and what this ticket therefore has to coexist with rather than change: a passages.source column with its own index and the own-words passages behind it (#33), the vector_codes table and the two-stage vector path (#30), and the migration ladder plus VectorSalvage (#34). None of them touch the query token set, so the design needed no revision — only new line numbers.
2026-08-31T20:21Z claude note MEASURED end-to-end on the real 477 512-passage index, all three arms against ONE copy of it so page cache and file layout are not variables (bench/results/0091-droplist/query-477k.json). Stock v1.12.0 answers the twenty queries at p50 222,0 ms and p95 392,5 ms, which lands on the 392,3 ms the X2 control read on v1.9.0 in August — harness, machine and earlier run agree. Delete the stoplist and prune nothing and the same file costs p50 965,6 ms and p95 1 132,8 ms: X2's failure mode reproduced on v1.12.0 — its MODE and not its magnitude, see the arm D note below — and the adoption case too, since that arm IS an index with no stored droplist. The droplist plus the fallback brings it to p50 282,0 ms and p95 696,5 ms. The tail is the fallback and not the pruning: take out the one fully degenerate query of the twenty and the same arm reads p95 491,7 ms, inside the allowance and near the 463,5 ms the sweep predicted for pruning alone. Fidelity: mean Jaccard against stock 0,866, no query emptied, and on the soliloquy query this arm returns EXACTLY the unfiltered result set where stock overlaps it on 5 items of 35. The derivation reproduces the sweep's 23 terms over 639 888 vocabulary terms, numerals 1, 2 and 3 among them exactly as the sweep's others_caught said, and it costs 2 020 ms on first call and 1 774 ms on the second — both on a page cache already warmed by whatever put the file in place, so both are floors and neither is the cold figure this ticket's ~7,5 s named; an earlier pair on the same machine read 2 281 and 2 013 ms, which is why the honest statement is two seconds and not a constant. Below the ~3,4 s this ticket carried either way.
2026-08-31T20:21Z claude note two findings the measurement produced that the design did not predict, both for the author. FIRST, the 30 % droplist is not a superset of the stoplist it replaces: it catches 19 of the 29 and adds 1, 2, 3 and s, leaving at, it, its, was, we, were, our, their, these and those — English function words common enough to be expensive and under 30 % document frequency in this library. Every query whose droplist p50 beats stock by more than 50 ms carries one of them, so the residual gap between arm B and stock is that list and nothing else. SECOND, the degenerate query's cost is INVARIANT to the threshold, by construction rather than by measurement: the fallback sends the raw token set, so a fully degenerate query costs the same at any cutoff, and lowering the cutoff only makes more queries degenerate. No threshold ruling can move the 696,5 ms figure; only a different fallback can — a phrase query over the raw set, or a cap on it — and that is DESIGN section 3 to rule on, not this ticket to invent. Method caveat worth carrying: queries-x2.txt was BUILT to carry at least three stoplist words per query, so it oversamples exactly the words a hand list catches and a df cutoff need not, and it is 5 % degenerate by construction, which over twenty queries is precisely where p95 lands. Read these as the adversarial case, not as ordinary traffic. A first attempt gave each arm its own copy of the index and read stock at 3 703,9 ms and 5 564,8 ms p95, nine to fourteen times the same binary on the other copy minutes later and rising between repeats; the two files are structurally identical (same page count, same freelist, same indexes, verified), so those readings are page-cache artifacts and are DISCARDED, which is why every figure above is same-file.
2026-08-31T20:26Z claude note PREPARED, not filed. Fork branch t0091-droplist pushed to FORK_REPOSITORY at 4510bb0, one commit atop b05ed69, green on upstream typecheck, eslint and 933 vitest tests. PR body drafted at verification/UPSTREAM-PR-0091-DROPLIST.md and its figures declared under the u0091 key in bench/check_figures.py, so a re-measurement cannot leave the one outward-facing document quoting a superseded number. Shape of the implementation, for the record: a new src/features/search/droplist.ts holding the ratio, the drift bound, the two minimum-survivor counts and the pruning function, so both backends share one policy and one place to read it; refreshDroplist and highDf as sibling hooks on SearchIndexBase beside finalizeVectors, called at the same three sites and for the same reason (a derived fact commits with the rows it was derived from); the SQLite backend deriving through a temp-schema fts5vocab table it drops again, so sqlite_master is unchanged and no schema bump is needed; the JSON backend answering from its own resident df instead, which is why it needs no stored list, no cadence and no adoption; and makeSnippet taking the droplist at a threshold of one surviving term where MATCH takes two. Left for the author: authorize the filing, and rule on the two questions above.
2026-08-31T20:56Z claude note review round 1 asked why the unpruned arm reads well under X2's 1 773,0 ms when the stock control reproduces X2 to 0,05 %, and the gap is now ISOLATED rather than explained away. Arm D: X2's own treatment binary — MinhHaDuong/zoteus stopwords-follow-up 94d994d, one commit atop v1.9.0, the exact tree that figure came from — built and run against this same file in this same session, reading p50 903,5 ms and p95 1 025,4 ms. Statistically the same as arm C's v1.12.0 code under the same policy. Same binary, same queries, same file, different day, so the v1.11/v1.12 search-layer rewrite is RULED OUT as the cause, which was the hypothesis worth killing since this ticket ships into that rewrite. What remains is the substrate and machine state between 2026-08-29 and today — the X2 run measured its own freshly built copy on a machine in a different state, this one shares a warmed file with three sibling arms — and that is named as the open question, not recorded as a residual: it is plausible and it is not established. Nothing here depends on it, because every arm is contemporaneous and same-file, so the ratios stand whatever August's absolute level was. Consequence for the prose, and it is a correction: say the failure MODE is reproduced, never the figure. Arm C also gained a second run (pooled p50 965,6 ms, p95 1 132,8 ms) since it was the one single-run arm and the one the review flagged.
2026-08-31T20:58Z claude note fork branch amended to eff9fb9 (supersedes 4510bb0) in review round 1: two figures inside the code and the docs were wrong in a way no gate here could see, which is itself the finding. droplist.ts cited the sweep's 10 % top-30 overlap as if it were THIS measurement, where the measured value on the degenerate query is 5 items of 35, and docs/semantic-search.md carried 53 bytes where the PR body carried 75 — the same quantity, one counting the term text and the other the stored row. Both live on the fork, so the u0091 declarations do not reach them and nothing but reading catches it. The rationale written for that key (the one text with a reader outside the repo) was therefore not quite true: the fork's own comments and docs go to him too, through the diff. Not fixed by a guard here, since the fork is not this repo's tree; recorded so the next upstream item reads its own diff for figures before pushing.
2026-08-31T21:06Z claude note the snippet criterion is now MEASURED on the real library, not only asserted in a fixture. verification/probes/snippet-droplist-probe.mjs opens the same 477 512-passage index twice through the shipped code — once with its stored droplist, once with the two meta rows taken out — and compares the snippet each arm returns for the SAME passage, discriminating mechanically on whether the snippet begins at character 0 (no leading ellipsis) rather than on anyone's judgement of readability. Of 91 hits both arms return, 82 begin at the passage opening unpruned and 45 do pruned, so 37 moved onto the match. Result at bench/results/0091-droplist/snippets-477k.json. Two things the examples make plain that the unit fixture could not: the opening a full-text chunk offers is a mid-word fragment, because chunks are cut at a fixed length and not at word boundaries, so the unpruned snippet is not merely unhelpful but visibly broken; and 45 still open at character 0 after pruning, which is the honest half — pruning halves the outcome, it does not abolish it, since a passage whose match genuinely sits near its start has nowhere better to centre. Written as a probe under verification/probes/ rather than as a bench driver because it settles a factual question and produced a committed figure; bench/query.py structurally cannot answer it, recording item keys and scores and never the snippet text, which is why this criterion had rested on a fixture alone until now.
2026-08-31T21:36Z claude note round 2 found the snippet probe's residue mixed immovable cases into it, so the probe now separates them — and the separation reads zero, which needed a positive control before it could be believed. makeSnippet returns a passage shorter than its 240-character window whole, with no ellipsis at either end, whatever the query, so such a snippet counts as opening at character 0 in both arms and no pruning could ever move it. In this population there are none: zero immovable pairs, and zero whole-passage snippets in either arm taken separately, which the probe now reports per arm precisely so a zero can be told apart from a predicate that never fires. The control that licenses reading it as absence rather than as a broken check: the same predicate over ten ordinary content queries on the same index finds 6 whole-passage snippets in 279 hits, 128 to 179 characters each. So the predicate fires, and the 45 that still open at character 0 after pruning are a real residue — a passage whose match genuinely sits near its start has nowhere better to centre — not an artifact of counting short passages. Two further method corrections in the same pass: the probe pairs by ITEM KEY and not by passage, because query reports one hit per item and never says which passage it centred on, so a pair is the same item's best hit read the same way on both sides, and the docstring said passage where it meant item; and the delete/restore of the two meta rows is now inside a finally, since a throw between them would leave a real index stripped of its droplist and silently change what the next measurement measures.
2026-08-31T21:40Z claude note round 2 close-out. The blocker it found is the same asymmetry round 1 fixed, one address over: GOVERNANCE.md's disclosure rule names pull request bodies, ISSUE TEXT and review replies, and this repo's form rule sends anything design-sized as an issue — so issue drafts are not the rarer outgoing class, they are the commoner one. verification/ISSUE-DRAFT-0024.md and verification/ISSUE-DRAFT-0488.md were committed, paste-ready and matched no covered glob, so a governance sentence in either passed clean AND the completeness check could not see them either. Round 1's fix was written from the document in front of it, which is exactly how FIELD-REVIEW.md spent months outside the gate. Now covered by prefix glob, both triaged into SCANNED, and three tests each shown RED against the pre-fix guard before being trusted — the first version of two of them was vacuous, one because it patched a module the test does not use (the suite loads its own copy through spec_from_file_location) and one because it monkeypatched SCANNED and so asserted only that scan reads SCANNED, which was never in doubt. Residue stated rather than hidden: a third naming convention would still be invisible, and globbing all of verification/ would close it while making every sibling PR red in combination, which is the failure round 1 had. Also closed: the head note's retracted sentence is now retracted IN the outgoing draft, not only in this log; the snippet probe restores in a finally and says which copy it mutates; the fork commit message is amended (f99beba, supersedes eff9fb9) since it ships to him with the diff and carried superseded arm C figures and a percentage derived from a warm reading; the ticket body's 53 bytes is annotated rather than changed, being the author's own figure and correct as term text; the artifact's verification block records the snippet result instead of calling it unmeasurable; and derive_droplist.mjs no longer labels its two readings cold and warm, since nothing in it controls the page cache — they are first call and second call, both floors, and the re-run at 2 020 and 1 774 ms against the earlier 2 281 and 2 013 is the evidence for saying two seconds rather than a constant. TERMINOLOGY.md gains droplist, which defines stoplist inside its own entry rather than earning a second one.
2026-09-01T08:53Z claude note checkpoint: this became a three-PR series in the author's order — fix the degenerate query, keep diacritics, then derive the list. Fork branches pr1-degenerate-r3 47461b7, pr2-diacritics-r3 f80d860, pr3-droplist-r3 bb5bb4c, stacked on base-v1.12.0, each standing alone with tsc, eslint and the suite green. Nothing filed upstream. State, settled findings with evidence, and the one open decision are in verification/0091-SERIES-CHECKPOINT.md: PR2's design needs a pinned 944-codepoint token class (unicode61 joins 944 combining marks and splits 1359 under remove_diacritics 0) and costs accented documents about 17% of their BM25 score; the untried alternative expands queries rather than documents and incurs neither.
2026-09-01T09:15Z PR 1 filed upstream as #45 (degenerate-query at 47461b7, body from UPSTREAM-PR-0091-DEGENERATE.md verbatim, gates re-verified 939 green). Rulings same day: one slot per PR (45 spends the held follow-up slot; PRs 2-3 need fresh grants), and PR 2's design decided by measuring the query-expansion arm before ruling.
2026-09-01T09:38Z handoff: PR 2 measurement campaign delegated to padme — repos at ~/CNRS/code/{search-works-for-zotero,zoteus}, frozen substrates under ~/data/projets/zoteus-bench, instruction HANDOFF-PR2-EXPANSION.md beside them; instance launches on the author's go, work lands on branch t0091-pr2-expansion (fork: pr2-expansion). Next after harvest: verify claims against artifacts, write the two-arm decision brief, then the PR 2 ruling and the PR 3 grant round.
2026-09-01T12:30Z claude note PR 2's design SETTLED by measurement per the ruling: query expansion wins over dual-token on every structural axis (0 % vs 23,5 % BM25 length penalty, no IDF distortion, migration 43,5 s vs 116,4 s, no 944-codepoint set, per-query cost ≤ 0,18 ms), refined with a corpus-derived dominance gate after naive expansion collapsed top-1 to 15/12/10/10 on rare accented siblings of common words. One premise disproven: the tokenizer declaration changes either way, so expansion does NOT avoid the migration — B3 fixed on the branch instead (non-corruption failure refuses and preserves the file, red-first). PR 3 reviewed (round 4, six findings fixed) then restacked on the settled PR 2 as pr3-droplist-r5, transform order prune-then-expand-survivors verified and asserted on both backends, full stack re-measured on the 477k corpus (arms-stack-*.json: EN pays ~30 % at the median with top-1 20/20; FR/VI at parity or faster). READY: pr2-expansion 6b7c152 (body UPSTREAM-PR-0091-DIACRITICS.md) and pr3-droplist-r5 11385a2 (body UPSTREAM-PR-0091-DROPLIST.md), gates green (958 passed), each awaiting a slot grant. Author's open ruling: whether the soliloquy running on its one under-bar survivor (not, 27,3 %) is acceptable or the fallback gets a ruleB-style refinement first.
2026-09-01T13:10Z claude note two rulings (ledger, same day): accent expansion becomes OPTIONAL, flag ZOTEUS_ACCENT_EXPANSION default on — pr2-expansion tip moves 6b7c152 → 6a201fa (appended commit, PR 3's stack on 6b7c152 unbroken; 958 tests green, 4 new covering both flag positions on both backends); and the author GRANTS the two remaining slots — PR 2 and PR 3 file upstream, bodies as drafted, after the repo-side record merges. Filing proceeds with the shipped zero-survivor fallback.
--- body ---
## Context
The measured replacement for the stoplist deletion that failed X2 (ticket
0014). The author's proposal, swept on the real 477 512-passage index:
**drop query terms whose document frequency exceeds the DESIGN §3 threshold,
and fall back to the raw token set when fewer than two terms survive.**

Measured, direct-SQL, against the two arms the abandoned PR was choosing
between:

| policy | p50 | p95 | matches unfiltered | degenerate query |
|---|---|---|---|---|
| stoplist (upstream today) | 215,2 ms | 365,8 ms | 86% | broken, 10% |
| stoplist deleted (the held PR) | 1 055,3 ms | 1 211,7 ms | 100% | fixed |
| droplist + degeneracy fallback | 274,2 ms | 536,9 ms | 98% | fixed |

It dominates both. It is also the only form that is well posed against a
shared token space: a per-language stoplist would have to drop German `die`
while keeping English `die`, an intent the index cannot see, whereas asking
what a string costs in *this* corpus has one answer
(`multilingual-cost.json`).

## The cadence question, settled

FTS5 does **not** hand over the high-df terms for free, which was the open
question. `fts5vocab` is a virtual table over the existing index — no
migration, no rebuild, nothing stored — but a df threshold needs a full scan
of it, because the table is ordered by term and not by document count. On
the real index that scan costs **~3,4 s warm and 7,5 s cold over 639 888
terms**. Too slow for query time, and too slow to pay at every server start.

So the droplist is derived state and needs somewhere to live. Everything
else about it is small: at the 30% threshold it is **23 terms and 53 bytes**
(term text; the stored row is 75 with its separators — the implementation
measured both, see the log), and `meta` (key/value) already exists in the
schema and already holds `schemaVersion` and PR-3's library stamp.

That fixes the cadence with no new machinery and no invalidation rule:

- **Recompute at the end of every full build.** The build already walks the
  whole corpus and costs 263,7 s; a 3,4 s scan is 1,3% on top, invisible.
- **Do not recompute on every incremental update.** A handful of new items
  cannot move a 30% threshold, and paying 3,4 s on a small update is the one
  way this becomes user-visible. Store the passage count alongside the
  droplist and rescan only when it has moved by more than ~10% since, so the
  cost is amortised against the drift that could actually change the list.
- **An index with no stored droplist behaves exactly as today**, filtering
  nothing, and adopts one on its first build. Same shape as PR-3's stamp
  adoption, and it means no existing index is stranded.

There is therefore no staleness question left for the author to rule on, and
no cadence knob to expose.

## Relevant files (upstream tree)
- `src/features/search/tokenize.ts` — holds the 29-word literal today.
  `tokenize()` must stay pure and language-agnostic; the droplist is a
  parameter, not a new global.
- `src/features/search/sqlite-index.ts` — `keyword()` at ~line 466 builds the
  MATCH string and already dedupes with `[...new Set(tokenize(q))]`; the
  `meta` helpers and `createSchema` are here.
- `src/features/search/bm25.ts` — `addDoc()` runs `tokenize()` over the
  DOCUMENT, so the memory backend must not silently inherit query-side
  filtering. Decide and test which side each policy applies to.
- `src/features/search/index-manager.ts` — `makeSnippet()` also tokenizes the
  query and centres on the earliest matching token, which is how the deletion
  broke snippets; whatever ships must be checked against that.

## Actions
1. Derive the droplist at the end of a build (and on the >10% drift
   condition) with one `fts5vocab` scan; persist terms plus the passage count
   in `meta`.
2. Apply it query-side only, after tokenize and before the MATCH string, with
   the degeneracy fallback at fewer than two surviving terms.
3. Delete the 29-word literal.
4. Decide the memory backend explicitly: it re-derives postings on load, and
   `addDoc` shares `tokenize()`, so state whether the droplist applies there
   and test the parity case either way.
5. Re-check snippets: with high-df terms dropped, `makeSnippet` should centre
   on content again — assert it.

## Test
Red first, on stock: a query of function words returns one incidental term
and unrelated hits. Then the fallback case, the parity case between backends,
the adoption case (no stored droplist filters nothing), and a snippet case.

## Verification
- [~] p95 on the 477k index inside the DESIGN §3 budget, re-measured with
      `bench/query.py --repeat 6`, not asserted. Measured, not asserted, and
      the answer is split: 491,7 ms over the nineteen ordinary queries, inside
      the allowance; 696,5 ms over all twenty, because the twentieth is the
      degenerate one the fallback exists for. See the log entry and
      `bench/results/0091-droplist/query-477k.json`. NOT a pass to claim
      whole — the author rules on whether the allowance is stated against a
      population containing a fully degenerate query.
- [x] degenerate query returns the same hits as an unfiltered query — on the
      real index, and identical rather than merely similar
- [x] an index built before this change filters nothing until rebuilt — arm C
      of the measurement is that index
- [x] snippets centre on content, not on the passage opening — measured on the
      real library, not only fixtured: of 91 hits both arms return, 82 open at
      the passage's first character unpruned and 45 do so pruned
      (`bench/results/0091-droplist/snippets-477k.json`). Halved, not abolished,
      which is the honest reading
- [x] upstream's own gates: typecheck, eslint, 933 vitest tests, all green

## Invariants
- DESIGN.md §3 owns the threshold (0090). This ticket implements it and
  restates no number.
- `tokenize()` stays language-agnostic: no language detection, ever — the
  homograph measurement is why.
- No existing index is stranded or silently re-ranked without a rebuild.

## Exit criteria
- Implemented on the fork, green on upstream's gates, p95 re-measured inside
  budget, and an upstream slot free. **The slot is the real gate**: ticket
  0016's cross-library wipe guard fixes silent data loss and outranks this,
  and the Zotero-native baseline showed matching to be the lower-value half
  of the layer. Do not spend a slot here while 0016 is queued.

## Where this stands
0016 merged as PR #32 on 2026-08-31, so its half of the gate is clear and both
slots of GOVERNANCE.md's volume bound are free (`SYNC.md`). The other half is
the author's: **nothing is filed upstream without his authorization**, so this
ticket stops at PREPARED. What exists is the branch `t0091-droplist` on the
fork (`FORK_REPOSITORY`), cut from `b05ed69` and green on upstream's own three
gates, and the PR body drafted at
`verification/UPSTREAM-PR-0091-DROPLIST.md`. The p95 half of the exit criterion
is measured and reported split, not claimed; the two design questions the
measurement raised are in the log for the author, and neither is this ticket's
to settle.
