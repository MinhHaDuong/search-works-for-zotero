%erg 0.1
Title: Reorganize sitter dialog into primary/details/diagnostics layers
Created: 2026-09-06
Author: claude

--- log ---
2026-09-06T00:00Z claude created
2026-09-06T00:00Z claude note Imagine phase (raid, sonnet): PROCEED. Confirmed the reorg is purely additive over the existing nested <details> (bootstrap.js lines 153-161 already nest sdt-index-details inside sdt-details) -- no restructuring of the section scaffolding implied. Recommended splitting the progress-toast addition into its own ticket (0696) since it is self-initiated (not author-requested, no DECISIONS.md ratification) and adds new accessibility surface on top of 0686's still-open a11y gap; done below. Sequencing: build the reorg skeleton and diagnostics content that's independent of 0689 (versions, admission readings, install path) now; wire a debug checkbox (new UI, bound to the existing `extensions.sdt-pack-sitter.debug` pref) and ring tail into the existing diagnostics disclosure once 0689 merges, with one final a11y re-verification against the completed layout rather than two passes.

2026-09-06T10:25Z sim note blocker 0689 closed — Blocked-by removed.
2026-09-06T00:00Z claude note Imagine phase round 2 (raid, sonnet): PROCEED WITH CHANGES. 0689 has now actually merged (it was still open at the prior Imagine pass), so the two-phase sequencing note is moot -- collapsed to one pass. Corrected "0689's debug checkbox" wording: 0689 shipped only the pref and trace-level gating, no checkbox UI exists anywhere in shipped code -- the checkbox is this ticket's own work to build, not a pre-built component to wire in.
2026-09-06T12:08Z claude note Built the three layers in bench/sdt-sitter/bootstrap.js: the primary sections unchanged and first, Détails (closed) gaining the observation count and covariate, Diagnostics techniques nested inside it and closed in its turn, hosting the debug checkbox, the ring tail, a copy action, versions, admission readings and the install path. Admission readings are recorded where blocked() reads them rather than on the refusing branch, so the layer is populated while the sitter is healthy. The copied journal carries the build and never the install path. Driven by tests/sdt_sitter_dialog.mjs against a stub document. Acceptance item 2 is NOT met by this branch — a real window needs a Zotero run — and is left to 0686, whose probe now carries the assertions.
2026-09-06T15:35Z claude note Privacy blocker from the PR #388 red-team fixed and pushed (74a1368, merged to 5d935c1). The ring survives a disable/re-enable via journal ??=, so the second initialize() emits 0688's startup record with rootURI INTO the ring, and composeSDTJournalReport serialised the tail verbatim. Now dropped from every record at the clipboard boundary, not merely omitted as a top-level field. The claim is narrowed to a true one: rootURI already reaches Zotero's submittable debug output by 0688's design, so what this branch buys is that its copy button opens no new door. The guarding test was vacuous and that is now demonstrated: restoring the defect against the shipped test file is GREEN, against the new arm RED. Admission-reading staleness surfaced as an age line. Manifest 2.6.2.
2026-09-08T08:04Z claude note TWO AUTHOR RULINGS ON THIS DIALOG, taken live on 2026-09-08 while he read the census on his own library, so they arrive with the evidence that provoked them. (1) THE CENSUS IS AN ACCOUNT AND SHOULD READ AS ONE: the total at the BOTTOM, and every category named in three to five words instead of one. Today it reads `unsupported: 2600 / missing-source: 367 / failed-session: 39` above a separate line "Could not be indexed (last census): 406" -- and that 406 silently adds 367 files that are merely not on this disk to 39 real failures. Two unrelated facts under one label. Not a cosmetic complaint: this session's own analysis went wrong twice on that figure before the breakdown was read, and the author reached for the same objection on sight. The classes do sum exactly (13 699 + 2 600 + 367 + 39 + 1 = 16 706), so it is honest arithmetic badly presented, which is the cheapest kind to repair. (2) "OBSERVED DURATIONS" IS TECHNICAL DIAGNOSTICS and belongs in the diagnostics layer this ticket already builds, not among the primary readings. The failure story behind ruling (1) is ticket 0740; this ticket owns only how the numbers are shown.
2026-09-08T16:08Z claude note Both rulings applied (PR #473). The census block now renders through describeSDTCensusAccount: eleven status-* labels of three to five words, rows in reading order rather than key order, total last and summed from the rows PRINTED rather than read off state.total, which matters because the two diverge mid-census. The conflating "Could not be indexed (last census)" aggregate is removed rather than reworded — every constituent holds a row of its own, so the account carries strictly more than the line it replaced, and the failure total a reader still meets is the layer-1 banner. sdt-observations moved from layer 2 to layer 3, leading it. Three controls fired against the committed tree (raw keys restored, total dropped, a label deleted). The session-clause guard was retargeted, not dropped: its pair is now diagnostics-completed against files-failed. Manifest 0.3.10. Acceptance item 2 stays unticked and now more so — the layout moved again, so the real-window a11y reading is owed against THIS layout; it remains 0686's scope.
2026-09-08T17:06Z claude note Round-1 findings land in PR #475, #473 having merged mid-review. Two blockers, both real. (1) The reading-order guard had a demonstrated false green: its `'([a-z-]+)'` read BOTH sides of the equality, so anything it could not match dropped out of both and the comparison agreed about a set it never saw — a double-quoted duplicate passed, and so did a duplicate renamed to `unsupported2` on both sides. Now one quote-backreferenced pattern over the full identifier class, with both red-team controls fired and reddening. (2) The #475 body claimed 0693 closed with #473; it did not. #473 carried a closing `**Ticket:**` line and the close step never ran, so the ticket sat open with no `Closed:` header and no close commit. #475 closes it — the third time in this repo that an unverified close claim was caught by someone re-reading origin/main rather than the branch.

--- body ---
## Context

Author, 2026-09-06: wants essential technical details hidden by default
but discoverable in the plugin's own window -- not removed, not exiled to
a log file only. Ticket 0686 already asked to "move technical diagnostics
below primary progress information." The dialog already has the right
skeleton: two existing `<details>` disclosures (`sdt-details`,
`sdt-index-details`).

## Design: three layers

1. **Primary** (always visible): progress bar, "Index 43 %" (0691),
   current document title, elapsed time, estimated finish, p5/p50/p95
   labelled "total scenarios" per SPEC.md R22.
2. **Details** (`<details>`, closed by default): the census account --
   every class named in three to five words, total at the bottom (ruling
   of 2026-09-08) -- last exceptions in plain words, native
   text-search-index stats (0691's renamed Zotero panel).
3. **Diagnostics** (`<details>` nested inside Details, closed by
   default): the observation count and the covariate used (per-page vs.
   per-byte), moved down from layer 2 by the ruling of 2026-09-08; the
   debug checkbox from 0689, the ring-buffer tail (last 50 records,
   auto-refreshed), a "copy journal" action, versions (plugin, Zotero,
   pack/schema/processor), admission readings, install path.

Nothing already shown is removed -- this is ordering, not deletion. 0689
has merged, so this is one build pass, not two: the debug checkbox and
ring-buffer tail (`Zotero.SDTPackSitter.journal.tail(50)`, already wired)
land in the same pass as the rest of the diagnostics layer.

## Acceptance

- [x] Diagnostics disclosure hosts a debug checkbox (new UI, bound to the existing `extensions.sdt-pack-sitter.debug` pref) and ring tail;
      opening the disclosure does not require debug mode
      to already be on.
- [ ] 0686's still-open accessibility items (keyboard open/focus/close/
      Escape/focus-return, reduced-motion, transition-only announcements)
      are verified in a real window against the reorganized layout, not
      re-approved from the old one -- verified once, after 0689's wiring
      lands, not twice.

      NOT MET by this branch, and it cannot be: a real window needs a Zotero
      run, which no headless lane has. What the branch does instead is make
      the verification executable — `verification/probes/sdt-sitter-harness/`
      now asserts the layer structure, the nesting, closed-by-default, the
      bound label, focus on each summary, and a ring tail readable with the
      debug pref off, and it records the reduced-motion reading and the
      toolbar's accessible name rather than leaving them to another reading
      of the source. The remaining items (Escape, focus return, reduced-motion
      behaviour, transition-only announcements) are 0686's own open scope and
      0686 stays open for them; this ticket's criterion pointed at 0686
      rather than duplicating it.
