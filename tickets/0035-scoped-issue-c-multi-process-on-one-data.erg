%erg 0.1
Title: Scoped issue C: multi-process on one data dir
Created: 2026-08-26
Author: claude
Blocked-by: 0032

--- log ---
2026-08-26T16:52Z claude created

2026-08-26T17:11Z claude note Copilot round 4: train + harness-offer ordering mechanized as Blocked-by
2026-08-28T16:24Z Claude note blocker 0015 closed — Blocked-by removed.
2026-08-29T09:58Z Minh Ha-Duong note blocker 0014 closed — Blocked-by removed.
2026-08-29T14:07Z Minh Ha-Duong note blocker 0017 closed — Blocked-by removed.
2026-08-31T14:53Z Claude note blocker 0016 closed — Blocked-by removed.
2026-09-02T06:33Z claude note THE FOUR-ROLE TOPOLOGY LANDS HERE when it goes upstream (DECISIONS.md 2026-09-02, two entries): the writer as its own process, the fetcher separate, one embedding service per machine. Design-sized, so an issue he builds himself (GOVERNANCE.md), and negotiated -- the author said so 2026-09-02. The one hook that sells it on his terms is his own #37: a writer out of the server process turns the Electron build crash from a search outage into a pipeline outage without knowing the cause. Never put this repo's reading of him into the text; state R6, R13, R22 and the RSS arithmetic and stop.
2026-09-02T08:01Z upstream #48 (2026-09-02, OpenAI 429 kills the embedding pass, resume re-embeds all) is field evidence for the worker/ledger split, and the topology still does NOT ride that thread: the draft comment at verification/ISSUE-48-COMMENT-DRAFT.md states only the invariant (a passage without a vector is pending work; a provider failure is a property of one request, so the embedding pass is killable and resumable wherever it runs) and stops. When this issue is drafted, #48 joins #37 as his own hooks: #37 argues the writer out of the server process, #48 argues the embed pass out of the build's lifetime
--- body ---
## Context
DESIGN.md §4 item 6-C, per the ratified re-form (DECISIONS.md 2026-08-26).
One #10-shaped issue for N zoteus on one data dir (R13): the empirical
SQLITE_BUSY reproduction (two handles, default timeout 0, immediate throw —
measured this cycle) beyond what PR-2's busy_timeout alone fixes; the
fulltext build's 500-item/60 s persist override (build.ts:321) holding the
write lock past any timeout — the per-page commit-cadence question moved here
from PR-2 so he can adopt it in his own idiom, with his update-path
single-transaction rollback explicitly respected; the conductor/lease sketch
(DESIGN §2.5) with the crash-only stdio reality stated in its favor (no
shutdown handlers on the stdio path, src/index.ts:73-76). Absorbs the
commit-cadence half of ticket 0016's original scope.

## Exit criteria
- [ ] issue drafted: repro + measurement + the cadence question, no code to accept
- [ ] filed after the PR train resolves; response recorded here; sunset applies
