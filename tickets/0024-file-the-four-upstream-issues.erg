%erg 0.1
Title: File the upstream issues (I-1..I-3; I-4 folded into 0033)
Created: 2026-08-26
Author: claude

--- log ---
2026-08-26T15:43Z claude created

2026-08-26T16:53Z claude note per the ratified re-form: I-2 framed as extending his existing #10 citation; I-3 behind the 6012 checkpoint; I-4 folded into scoped issue A (0033), not filed alone

2026-08-26T17:05Z claude note body reconciled with the re-form (code review): I-4 bullet and exit criterion removed, §4 item renumbered
2026-08-27T15:52Z claude note re-verified at v1.8.0 (309204b): I-1 STANDS — action:"update" still keys since on libraryVersion (build.ts:280); 2f453d6's metadata-first work (#23) withholds the stamp from interrupted crawls but never revisits unchanged items with post-build extraction. I-1's text should cite #22/#23 as neighbors (his own follow-up territory) — SYNC.md §4 tail note. I-2 STANDS — 2f453d6 rewrote the repair and metadata-first doc sections, measurement claims untouched. Upstream numbers #21-#23 are consumed; the I-labels stay internal and assume no numbers
2026-08-27T19:05Z claude note I-1 and I-2 paste-ready texts composed from the ticket scope + SYNC.md sections 4-5 with the v1.8.0 re-verification folded in (I-1 cites the 0012 artifact and offers X6's measurement; I-2 leads with the prototype-not-his-backend caveat and carries both memory numbers) and handed to the author for filing. I-3 stays behind the 6012 checkpoint. Upstream formatting used in the paste texts, not this repo's decimal comma
2026-08-27T20:20Z author rejected both texts for now: I-1 closes with an offer to measure on the workstation — impossible today, and the standard is DO the measurement, not offer it; I-2 injects numbers measured on the fork prototype branch where a correction to his docs must carry numbers measured on trunk. CONSEQUENCE: I-1 waits for X6 (its answer goes IN the issue, replacing the offer); I-2 waits for a trunk re-measurement at v1.8.0 through the renamed drivers (0030). Both ride the next workstation session; the prefilled URLs for the two issues are withdrawn
2026-08-28T08:06Z claude note upstream #24 (@StianOby, 2026-08-27, resume interrupted local-API build — no usable library version stamp) is third-party demand on I-1's seam, the batch trigger per SYNC.md's pattern. Cite #24 as a neighbor in I-1's text alongside #22/#23. Open question for the author: file I-1 standalone (the ratified form) or fold it as an annotation on #24's thread — distinct defects (resume vs post-build extraction invisibility), so a fold risks the finding reading as a footnote to someone else's symptom; SYNC.md §4 records the same
2026-08-28T08:19Z claude note the comment on #24 (from here) read and verified accurate at 309204b: reset() unconditional at index-manager.ts:546, start=0 at :578, cadences 200/10s and 500/60s confirmed. It stays inside #24's scope — keeps libraryVersion as the freshness signal, does not surface I-1. Recalibration: the fold option is moot (the comment exists and withholds the finding); its checkpoint/freshness separation makes I-1 sharper as a standalone issue — once resume is decoupled, the remaining freshness hole IS I-1. Recommendation stands: file I-1 citing #24 and the comment's separation
2026-08-28T08:23Z claude note I-1 text prepared FINAL (2026-08-28) and delivered in-session, ready to file: mechanism cited at 309204b (build.ts:280 since=libraryVersion with top:true; updateIncremental fulltextFor only for the changed set, index-manager.ts:790), the 0012 artifact linked (410 vs 0..25036, since=410 returns 93% of text), the unmeasured direction offered as a verification protocol, #23/#24 and the checkpoint separation cited as neighbors, a narrow fulltext-cursor option sketched, regression test included. No internal labels in the text
2026-08-28T08:34Z claude note I-1 FILED 2026-08-28 as oscardvs/zoteus#26, verbatim from the prepared text, via a dedicated sibling session (this session cannot reach the upstream API). Body verified publicly: all sections, the 0012 artifact link, build.ts:280-284 and index-manager.ts:790 citations intact; author MinhHaDuong. Exit criterion checked. Remaining: I-2 (ready), I-3 (behind the 6012 checkpoint); record his response here when it lands
2026-08-30T08:19Z Minh Ha-Duong note I-2 WITHDRAWN, not filed — ruling in spec/DECISIONS.md (2026-08-30): noise once upstream adopted the sqlite backend; docs/semantic-search.md at v1.10.0 names the JSON ceiling's mechanism and carries measured figures of its own, so the correction has no target left. The redrafted text (trunk v1.10.0 numbers, drafted 2026-08-30 after RUNBOOK step 4) is discarded; the trunk artifacts stay as repo-side evidence (bench/results/trunk-1.10.0/, ticket 0025's log); ticket 0460 holds the source-level inventory of the ceiling

2026-08-30T17:04Z claude note I-3's #6012 gate is DISCHARGED by the checkpoint read at source 2026-08-30 (full reading in 0028's log): the PR is draft, unmerged, unmoved since 2026-08-29, and touches no server/API/connector file, so the platform will not absorb the truncation question and I-3's premise stands. This clears the gate; it does not authorize the filing. Per CLAUDE.md every upstream API action needs the author's authorization, and GOVERNANCE's volume bound and contained-PR budget are SYNC.md's to report against. AWAITING THE AUTHOR: file I-3 now, or hold it. Note one thing that has changed under it since the ticket was written -- the 40k cap is zoteus's DEFAULT_FULLTEXT_MAX_CHARS, and tickets 0483 and 0480 now hold the extraction-cap and staleness inventories that would supply its measured evidence, so the text should be composed from those rather than from the ticket's original framing
2026-08-31T05:49Z claude note README-LEVEL EVIDENCE for I-3, read at the reviewed baseline (upstream README.md at v1.10.0, HEAD = b132f2d, no drift from UPSTREAM): the product README promises that with ZOTEUS_INDEX_FULLTEXT on, "semantic search covers the body of every PDF too, so a claim that never made it into an abstract is still findable". Unqualified, in the headline feature list. Against it, DEFAULT_FULLTEXT_MAX_CHARS in src/features/search/fulltext-source.ts carries its own scoping comment: "~13 pages of dense text, so a typical paper is covered end to end" -- honest about the case it was sized for, and silent about the case R9 exists for. So the cap is not a hidden default; it is a default whose documented scope ("a typical paper") and whose advertised scope ("every PDF") disagree, and the 44.9 MB living example is where they part. USE FOR THE I-3 TEXT: this is the symptom a user can check without reading source, and it opens the issue on his own words rather than on our reading of his code; tickets 0483 and 0480 supply the mechanism and the measured evidence, per the 2026-08-30 note. Still AWAITING THE AUTHOR's authorization to file -- this note changes the text's opening, not the gate.
2026-08-31T07:25Z claude note I-3 TEXT COMPOSED and committed as verification/ISSUE-DRAFT-0024.md, paste-ready, NOT SENT -- still awaiting the author's authorization, which the 2026-08-30 checkpoint note did not grant. Composed per that note from tickets 0483/0480 plus the README opening logged 2026-08-31, and hardened against the one-line dismissal it invites ("just set the limit to 0"): the setting requires the knowledge the silence withholds, zero is what the 2 046,1 MiB row measures so it is not generally recommendable, and a per-install env var cannot make a shipped default honest. Carries measured evidence only, no offers: X3a's uncapped build at v1.10.0 (bench/results/0025-x3a-monster-rss/), the at-rest contrast, and 0483's pdfMaxPages probe stated at its settled half only (textMaxLength stays unclaimed). Opens on his own README sentence rather than on our reading of his code, and cites his own itemsAvailable/itemsTotal comment as the precedent the body cap lacks. Carries a numberless heads-up on entry-aligned chunking, which 0034's log permits: a seam for the streaming shape, no segmenter number, no offer, X5 still open. No document names, no item keys, no internal labels, no ticket numbers, upstream number formatting per the 2026-08-27 convention. File is pure issue text with no repo-side preamble, deliberately: a body pasted from a file that carries anything else is how a session trailer once reached a public PR
2026-08-31T07:36Z claude note I-3 DRAFT REVIEWED 2026-08-31, not sent. Every quoted figure re-verified against bench/results/0025-x3a-monster-rss/ (2 046,1 MiB peak, 182,2 s, 477 511/465 109, 938,8 + 147,4 MiB, 7 541 items, 122,7-137,1 MiB at rest, 7,7 ms warm p50) and every code quote re-verified against upstream at the reviewed SHA b132f2d by fresh clone: fulltext-source.ts:189 slice line, :173 the cross-attachment break, the DEFAULT_FULLTEXT_MAX_CHARS comment verbatim including '~13 pages of dense text', backend.ts:188-193 itemsAvailable and its doc comment, truncationNotice at build.ts:100, zotero_get_fulltext, and the README sentence. The 43,3 MiB extracted-text figure and the pdfMaxPages probe (indexedPages 100 / totalPages 114) both trace to committed artifacts. TWO DEFECTS FOUND AND FIXED. (1) SERIOUS: the draft said 'I am prototyping entry-aligned chunking on a fork'. We are not -- seg/1 does not exist, 0028's build criteria are all unchecked, X5 is unrun, and SYNC.md's fork branch list carries no segmenter branch. That is a false claim about our own state, made in public to the maintainer, and it is the 2026-08-27 rejection's class made worse: not offering future work but asserting present work. Corrected to 'designed and not yet built, and nothing about it is measured'. (2) The at-rest paragraph quoted 7,7 ms p50 alone while the same run's warm p95 is 210,5 ms -- a 27x gap, in a paragraph arguing serving cost is not the problem. A maintainer who runs it and sees 210 ms reads the number as chosen. Both figures now stated, per the both-numbers-always discipline the README already applies to memory
2026-08-31T07:44Z Minh Ha-Duong note I-3 WITHHELD 2026-08-31 — not filed. The #6012 checkpoint cleared its gate and the reviewed draft stands correct at verification/ISSUE-DRAFT-0024.md, but authorization is withheld: the text waits rather than being withdrawn, so this is a hold and not I-2's fate. The exit criterion stays unchecked deliberately. Nothing further is owed unless the author lifts the hold; if he does, the draft is paste-ready as reviewed, with the two 2026-08-31 corrections already in it (the false prototyping claim, and the p50-without-p95)
2026-08-31T14:54Z claude note upstream advanced to v1.12.0 (b05ed69) 2026-08-31, two releases past the reviewed baseline. I-1 was filed as #26 and closed COMPLETED in the v1.10.0 sweep; the standing re-verification recorded here is against v1.8.0 (309204b) and is now four releases stale. Any further re-verification is against v1.12.0 and belongs with the re-baseline, ticket 0504 — build.ts took +137 and index-manager.ts +454 between v1.10.0 and v1.12.0, so the since/libraryVersion reading I-1 rests on cannot be assumed to survive unread
--- body ---
## Context
DESIGN.md §4 item 4. Form per SYNC.md's measured asymmetry: design-sized
problems go as issues he builds himself (#10, two for two).
- I-1: the fulltext-delta / mixed-sequence question — drafted FINAL in the
  cycle-1 session, upgraded to a FINDING in cycle 2: startIndexUpdate keys on
  libraryVersion alone, so post-build extraction is invisible to
  action:"update". X6 (0025) is its empirical annex.
- I-2: the measurement corrections for docs/semantic-search.md — WITHDRAWN
  2026-08-30 by ruling (spec/DECISIONS.md), see the log.
- I-3: the 40k truncation cap vs R9 — DEFAULT_FULLTEXT_MAX_CHARS = 40_000
  truncates the 44.9 MB living example ~1,100-fold; design-sized (streaming),
  his to build.
- I-4 (legacy search-index.json retirement) is NOT filed alone: folded into
  scoped issue A's custody paragraph (0033) per the ratified re-form.

## Exit criteria
- [x] I-1 filed 2026-08-28 as upstream #26 (finding wording, 0012 artifact linked, #23/#24 cited)
- [x] I-2 withdrawn, not filed — ruling 2026-08-30 (spec/DECISIONS.md); trunk artifacts stay as repo-side evidence
- [ ] I-3 filed citing the measured truncation (after the #6012 checkpoint)
- [ ] responses recorded here as they land
