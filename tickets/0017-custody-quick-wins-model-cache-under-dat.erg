%erg 0.1
Title: Custody quick wins: model cache under dataDir, Gemini key out of the URL
Created: 2026-08-26
Author: claude

--- log ---
2026-08-26T15:43Z claude created

--- body ---
## Context
DESIGN.md §4 PR-4 and PR-5, honoring R28 and R10. PR-4: pin the transformers
cache (env.cacheDir) under dataDir before constructing the pipeline — the
default lands weights outside dataDir, breaking uninstall-by-deleting-dataDir;
one line now versus gigabytes of surprise later. PR-5: the Gemini embedder key
travels in the URL query string (embeddings.ts) — move it to the header.
Cheapest custody wins; they also build merge history before the RFC (0027).

## Exit criteria
- [ ] PR-4: weights land under dataDir; uninstall test deletes everything
- [ ] PR-5: key in header, no key in any logged/thrown URL
- [ ] both PRs opened upstream; outcomes recorded here
