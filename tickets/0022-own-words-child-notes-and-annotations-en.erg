%erg 0.1
Title: Own words: child notes and annotations enter the corpus
Created: 2026-08-26
Author: claude
Label: deferred

--- log ---
2026-08-26T15:43Z claude created

2026-08-26T16:53Z Claude label deferred
2026-08-26T16:53Z claude note reserve list per the ratified re-form: open only on a warm batch or third-party demand
2026-08-29T13:58Z claude note FILED AS ISSUE #33 upstream (2026-08-29), not as PR-10. Built first on fork branch own-words-notes-annotations (50245d3, one commit on b132f2d): source label widened to 'fulltext'|'note'|'annotation' with no schema change, passages carry the PARENT item's key so query()'s per-item dedup keeps own words extending rather than diluting, own-words census is one paged itemType=note||annotation read plus a batched itemKey= lookup per fifty annotated attachments, and the update path asks ?since= of the children because annotating leaves the parent's version untouched (same class as #26, stated conditionally in the issue — the version claim was NOT measured). Gates re-run here independently: typecheck, lint clean, 865 passed / 7 skipped. Counter-proof: nine tests using only stock's public API fail on v1.10.0 as blindness (empty result arrays), not as missing counters. FORM CHANGED because the build came out design-sized: 17 files, +1050/-22, against +10/+6/+77/+368 for the four contained PRs merged or in flight — and it carries two product decisions the maintainer owns (own words on by default, changing every user's index size; whether they ride the fulltext pass). The measured asymmetry says design-sized work is built by him from an issue, now five for five. Issue leads with the zotero_annotate asymmetry (v1.10.0 writes annotations no query can find), carries the two probed local-API facts, and points at the branch as a prototype explicitly not offered as a PR. Spends no in-flight slot
2026-08-31T05:49Z claude note README-LEVEL EVIDENCE for #33, read at the reviewed baseline (upstream README.md at v1.10.0, HEAD = b132f2d, no drift from UPSTREAM): the product README claims, under "Find anything in your own work", "full-text keyword search inside your PDFs and notes". At that same commit src/features/search/ contains no note or annotation handling at all -- grep for note/annotation across backend.ts, build.ts, fulltext-source.ts, index-manager.ts, chunker.ts returns nothing -- so R16 is not merely undesigned upstream, it is documented-and-absent. #33 argues from the crawl (top:true) and from the zotero_annotate asymmetry; this is a third and cheaper argument, checkable by a user who never opens the source. Hold it for the response thread rather than amending the filed issue: the issue is already lodged and re-litigating its framing spends goodwill for nothing.
--- body ---
## Context
DESIGN.md §4 PR-10, honoring R16 and D7 (both notes and annotations). Verified
gap at edf2748: builds crawl top:true only — standalone notes are indexed,
child notes and annotations are not, so the researcher's own words are
invisible while zoteus can create annotations it can never find again. The PR:
an itemType-filtered second crawl pass for child notes + annotations, #6012
eligibility cited (they embed both as first-class).

## Exit criteria
- [ ] child notes and annotations indexed; failing test shows today's blindness
- [ ] record-core ruling respected: own words extend, never dilute — via source label widening (`source: 'note'|'annotation'`); per-field columns stay with the entries machinery (0034)
- [ ] PR opened upstream; outcome recorded here

## Amendments (implementation review, 2026-08-26)
Scope correction: "own field/column" cannot mean an FTS column upstream — the
schema has one text column. It means a source: 'note'|'annotation' label
extending the existing source: 'fulltext' pattern (backend.ts:38 widens). The
crawl pattern to copy is fulltext-source.ts:87-92 (itemType: 'attachment');
the local client passes itemType through (local-client.ts:99-101).
