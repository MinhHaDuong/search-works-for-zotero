%erg 0.1
Title: Journal sitter state transitions through Zotero.debug and an in-memory ring
Created: 2026-09-06
Author: claude
Closed: autoclosed — PR #372

--- log ---
2026-09-06T00:00Z claude created
2026-09-06T00:00Z claude note Imagine-phase finding (raid, model sonnet): the code merged in PR #365 already writes an unbounded, never-rotated `sdt-sitter-errors.jsonl` on every failure (bootstrap.js's `reportError` host callback, via `IOUtils.write(..., {mode:'append'})`). That is the exact durable failure ledger the 2026-09-05 ruling and SPEC.md R22 forbid, predates this ticket, and isn't mentioned in the original body. Folded into scope below as in-scope cleanup, not new scope -- removing an existing write isn't "writing a new file" under this ticket's own acceptance bar.

2026-09-06T08:53Z claude note Execute: no persisted file added, so the flag-back to the author is not triggered. Zotero.debug() carries error and state always; the pref extensions.sdt-pack-sitter.debug (read fully qualified, Prefs.get(name, true)) gates trace only. native-worker-busy emits worker-idle-wait at trace, every other gate reason emits refuse at state, instrumented once in scheduler.js and never also in bootstrap's blocked(). sdt-sitter-errors.jsonl and its IOUtils.write are gone. Eleven control arms on a committed tree, each firing red; two found tests that could not fail and were rewritten to drive production code. SPEC.md left untouched: its sitter section already says failures are suppressed without a private durable ledger, and a paragraph on the diagnostic channel needs the date-is-version bump, so it is proposed as a follow-up rather than folded here.
2026-09-06T09:01Z claude note SPEC.md deferral is now tracked by ticket 0698, not by this log line alone.
2026-09-06T10:07Z claude note Review round 2: the error field carries the error's class only, never its message. Three reviewers each found a leak in successive attempts to scrub the message token by token, which is the tell -- a stored filename is 'Author - Year - Title.pdf', and a pattern anchored on runs of non-whitespace can only redact the token touching the extension. The message is now not carried at all; describeError still shows the author everything on screen, locally. classifyError carries its own try/catch because it runs as emit()'s argument, outside emit's guard, and a torn-down compartment can throw from a getter. shutdown() is try/finally so a throwing dialog.close() during app teardown cannot leave the channel open and unrecorded.
2026-09-06T10:07Z claude note Two consider-level findings recorded, not fixed: (a) a stale generation's suspended initialize() can write one extra cache-load record -- a count, no identity -- into the live ring across an unguarded await gap; bounded and self-correcting at the next token check. (b) ring retention under heavy progress traffic during a long sweep is open, since the native callback's cadence is not this repo's to set; 2000 records is a guess until measured.
2026-09-06T10:09Z claude note Manifest goes to 2.4.0, not 2.3.0: PR #373 bumps the same string over a different payload and adds bench/check_sitter_version.py to make check, which fails on two payloads sharing one version. The bump is also the last commit of the series, since a later bootstrap.js edit under an already-bumped number reds that same guard against this branch alone.
2026-09-06T10:18Z claude note Version is 2.4.1, not 2.4.0: the merge with origin/main changed the payload again (0691's describeSDTFile wording, and the M4 anchor restored in scheduler.js), and #373's guard compares the working tree's version against every earlier revision of bench/sdt-sitter -- an earlier commit carrying 2.4.0 over a different payload is exactly what it fires on. Verified by running that guard from the #373 branch against this tree: OK, ahead of all 27 earlier revisions.
2026-09-06T10:25Z sim closed — autoclosed — PR #372
--- body ---
## Context

The author wants a debug mode that journals execution flow and critical
states, specifically to diagnose hangs and crashes -- the sitter's whole
point is to supervise Zotero's native worker, so its own fragility defeats
its purpose (author, 2026-09-06).

An initial pass (drafted with a Fable 5.1 synthesis, see the "Sitter's
Ledger" review artifact) proposed a wholly new, rotating file,
`sdt-sitter-debug.jsonl`. That conflicts with two things this project has
already settled, and should not ship as designed:

1. **A ratified project decision.** DECISIONS.md, 2026-09-05 (RATIFIED,
   experimental overnight native SDT sitter): "Failures are suppressed for
   the session by source and processor identity, **without a private
   durable ledger**." A persistent debug/failure file is exactly the durable
   ledger that ruling forbids, unless the author explicitly carves out an
   exception for it.
2. **Zotero's own platform convention.** `Zotero.debug()` is the standard
   plugin logging call, viewable via Help -> Debug Output Logging -> View
   Output. Zotero's own debug output is deliberately **not** persistent --
   per Zotero's documentation it "needs to be started/cleared for
   performance and memory reasons." Building a parallel, persistent
   channel goes against the platform's own choice for this exact class of
   diagnostic data, and it also means the author's existing habit of
   opening Debug Output Logging to look at a plugin problem would not see
   the sitter's journal at all.

## Design

- **Primary channel: `Zotero.debug()`.** Emit the sitter's state-transition
  vocabulary through it -- `admit()`, `submit()`, `progress()`, `settle()`,
  `refuse()`, `worker-idle-wait`, `dialog-open/close`, `cache-load`,
  `cache-write`, `shutdown()`, and a `heartbeat` every 60s while active
  (phase, elapsed time on the active document, pending count). No titles,
  no source paths -- same privacy rule as the existing cache.
- **In-memory ring buffer, always on, never written to disk by default.**
  2,000 records. This is what makes a hang diagnosable *after* it happens
  without violating the no-durable-ledger ruling: nothing here survives a
  crash, but the last 2,000 transitions are there the moment the author
  opens the panel. Surfaced in the dialog's diagnostics disclosure (see
  0693) with a "copy" action.
- **Retire the existing `sdt-sitter-errors.jsonl` write.** `bootstrap.js`'s
  `reportError` host callback currently does `IOUtils.write(errorPath, ...,
  {mode: 'append'})` on every failure -- an unbounded, cross-session,
  never-rotated file that is already the durable failure ledger the
  2026-09-05 ruling forbids. Replace that callback's body with the new
  `settle`/failure event into the ring + `Zotero.debug()`; do not leave the
  old write running alongside the new compliant channel.
- **A single pref**, e.g. `extensions.sdt-pack-sitter.debug`, gates only
  the `trace`-level detail (heartbeats, per-probe resource readings) going
  to `Zotero.debug()` -- `error` and `state` levels always go through it,
  matching how "Report Errors" behaves versus "Debug Output" in Zotero's
  own UI.
- **If the author wants a persisted file anyway** (surviving a full Zotero
  crash, which the ring buffer cannot), that is a new, explicit ruling on
  top of the 2026-09-05 no-durable-ledger decision, not something to add
  silently in this ticket. Flag it back to the author rather than deciding
  it here.
- Hang discrimination: the panel shows "last progress N min ago" read from
  the ring; it never renders a hang verdict -- ticket 0681 owns deciding
  what a *proven* worker death is, and reads this journal as its evidence
  channel.

## Acceptance

- [x] After a synthetic hang, the ring's tail names the active document
      identity, the last progress tick, and every heartbeat since --
      verified by reading it, not by asserting it exists.
- [x] `Zotero.debug()` calls never throw into the sitter loop.
- [x] Disable mid-job writes `shutdown(disable)` as the last record, with
      nothing after -- evidence the callbacks were really removed.
- [x] No new file is written to disk by this ticket. If that changes, the
      ticket log records the author's explicit go-ahead first.
- [x] `sdt-sitter-errors.jsonl` and its `IOUtils.write` call are removed;
      no code path writes it any longer.
