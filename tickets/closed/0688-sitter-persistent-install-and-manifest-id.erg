%erg 0.1
Title: Install the SDT sitter persistently and fix its manifest identity
Created: 2026-09-06
Author: claude
Closed: autoclosed — PR #373

--- log ---
2026-09-06T00:00Z claude created
2026-09-06T00:00Z claude note Imagine phase (raid, sonnet): PROCEED. Reuse bench/acceptance/adapters/beaver.py's existing extensions.json install/verify pattern (lines ~338, 614, 913-941) instead of hand-rolling it -- it already does persistent XPI install + present/absent/version/active/location reporting for a different plugin, defensively (missing/unreadable file reported "unknown", never coerced false). Don't block the startup self-check on 0689 landing first: emit it via one direct Zotero.debug() call now (no such call exists yet in bootstrap.js), let 0689 fold it into its vocabulary later. Add explicit acceptance checkboxes for the startup self-check and version-bump-test Work items, or justify why the existing three cover them.

2026-09-06T08:54Z claude note Execute phase implemented on t0688-sitter-persistent-install. Persistent install (bench/sdt_sitter_install.py, make sitter-install/sitter-verify-install), update_url now names a committed bench/sdt-sitter/update.json served raw from main, a startup Zotero.debug() self-check in initialize(), and a version guard (bench/check_sitter_version.py, in make check). Version bumped 2.2.0 -> 2.3.0.
2026-09-06T08:54Z claude note Finding while writing the version guard: four commits (0715d2d, c383556, fb0239f, ef59cb1) each shipped a DIFFERENT bench/sdt-sitter payload under manifest version 2.2.0. Zotero keys its add-on record on that number, so the author has had several distinct plugins answering to one version -- a third candidate mechanism for the disappearance, alongside the two the body names, and the one this ticket's guard now prevents.
2026-09-06T08:54Z claude note Zotero docs read (zotero.org/support/dev/zotero_7_for_developers): update_url appears in the example manifest but is nowhere stated as required, and only applications.zotero itself 'must be present'. Removing it was therefore permitted but changes two variables at once on a plugin already disappearing, so it is KEPT and made resolvable. STILL OPEN: the discriminating question in the body (did it vanish right after a Zotero update, and was it installed via Tools -> Add-ons -> Install Add-on From File) is unanswered; the acceptance box stays unticked. The work covers both mechanisms, so the answer is no longer a precondition for the code -- only for closing.
2026-09-06T09:03Z author answered The plugin disappeared with NO Zotero update in between, and it had been installed via Tools -> Add-ons -> Install Add-on From File -- the persistent path, not a temporary/dev sideload.
2026-09-06T09:03Z claude note Both named mechanisms are RULED OUT by that answer: no Zotero update happened (kills 'administrative incompatibility'), and Install Add-on From File is the persistent path that copies into the profile's extensions/ (kills the temporary-add-on lifecycle). The cause is therefore OPEN -- deliberately not replaced with a third guess. What this PR changes is that the next occurrence leaves evidence: the startup Zotero.debug() line says which build was running, and 'make sitter-verify-install' distinguishes the three states that currently look alike -- entry present but active:false (the host disabled it), entry gone but the XPI still in extensions/ (the host rejected or dropped the record), and the XPI itself gone (something removed the file). Closing this box as answered-and-recorded, cause open, needs live reproduction.
2026-09-06T10:47Z claude note Review round 1 (correctness plus red team, both request-changes, every finding reproduced live): a JSON document of the wrong shape (a top-level array) escaped read_addon_record as AttributeError and exited 1 -- this tool's ABSENT code -- collapsing 'could not read' into 'absent'. report() and main() had no test at all, which is how it shipped, so the exit codes 0/1/3 are now driven through the CLI. The reuse check compared versions as text while the regression check beside it parsed them, so 2.03.0 slipped a changed payload past both. The guard printed OK on a repository with zero commits for the payload path, and on a replace-grafted history -- two more routes to the blindness its shallow fix had closed by one route only. --addon-id was interpolated into a path unsanitised. And the startup self-check wrapped only the manifest read, leaving Zotero.debug and the Zotero.version getter able to abort the very startup it exists to explain. All fixed, each with an arm that fails against the pre-fix line: six controls run, six red.
2026-09-06T10:52Z claude note Merged origin/main (44 commits, the whole sitter wave). Two conflicts, both resolved as a union: manifest version to 2.5.0, above 0689's 2.4.1, keeping its description of the journal; and tests/test_sdt_sitter.py, keeping 0691's UI-vocabulary suite alongside this ticket's installation suite. One substantive rework rather than a mechanical resolve: 0689 landed emit(), a guarded diagnostic channel that seals at shutdown, so the startup self-check now goes through it as an emit('startup', ...) record instead of a Zotero.debug() of its own -- one channel, and the fold the Imagine note deferred to 0689 which 0689 did not make. Every read inside the self-check is guarded because it runs as emit's argument, outside emit's own guard, which is the hazard 0689's classifyError comment documents. Union grep-verified both ways after the merge.
2026-09-06T11:37Z claude note Review round 3 -- two new blockers, same class, both reproduced with controls firing first. The three-valued read collapsed once more one level deeper: document shape and container shape were checked, and an ELEMENT whose id is a number still met sorted() over mixed types, raising TypeError out of main() into exit 1, ABSENT again. RecursionError on deep nesting was a second door, and the arm written for it was VACUOUS at depth 20000 -- CPython 3.14 parses that fine -- until measured and resized to 200000. The guard also answered OK about a repository it had not read: GIT_DIR and its relatives were inherited unfiltered, so --root named the tree while the environment quietly named the history. A partial clone was the other half, since show fails identically on a filtered blob and on a path that never existed, and only the tree can say which. Five controls run, five red. Filed 0712 for the same three defects in beaver.py's _host_addon_record, which this reader was modelled on: reproduced there too, controls first, and left to its own ticket rather than folded in.
2026-09-06T11:41Z sim closed — autoclosed — PR #373
--- body ---
## Context

The author reports the sitter "tends to disappear on its own from the
installed-plugins list." `bench/sdt-sitter/` does not exist on `main` yet
(it ships only in PR #365), so this ticket cannot start coding until #365
merges -- but the two candidate mechanisms below are worth recording now.

Ticket 0680's own closing note says the XPI was "delivered to author home
without profile installation," and its exit criteria state "No automatic
installation into the author's live profile by the test harness." That is
consistent with either of two independently real Zotero mechanisms, neither
confirmed yet against this specific incident:

1. **Non-persistent install.** If the XPI was ever loaded as a temporary/
   unpacked add-on rather than through Tools -> Add-ons -> Install Add-on
   From File, Firefox/Gecko's temporary-add-on lifecycle unloads it on
   restart or source changes -- this is not a crash, it is how that install
   path works.
2. **"Administrative incompatibility."** Zotero disables (and, per forum
   reports, sometimes fully removes) a plugin whose `strict_max_version`
   does not cover the running Zotero build, on ordinary point-release
   updates. See https://forums.zotero.org/discussion/133127/ and
   https://forums.zotero.org/discussion/94617/. The manifest's
   `strict_max_version: "10.*"` should tolerate Zotero 10.x point releases,
   but the exact range-matcher behavior is not verified here.

Separately, `bench/sdt-sitter/manifest.json`'s `update_url` points at
`https://example.invalid/...` -- an RFC 2606 address guaranteed to never
resolve. Zotero's own developer docs list `update_url` as a required
manifest field but do not document what an unreachable one does at
runtime; this is unconfirmed as a cause, not ruled out.

## Before implementing

Ask the author the discriminating question this ticket cannot answer from
the repo alone: did the plugin vanish right after a Zotero version update,
or with no Zotero update in between? And was it installed via Tools ->
Add-ons -> Install Add-on From File, or some other way? The answer picks
between mechanism 1 and 2 above before more effort is spent chasing either.

## Work

- Install as a persistent extension: copy the XPI into the profile's
  `extensions/` directory under the manifest id (or install through
  Zotero's Add-ons dialog), not a session sideload. Add an `install` /
  `verify-install` Make target that reads the profile's `extensions.json`
  for the manifest id and reports present/absent.
- Fix `update_url`: either drop it if Zotero's manifest validator allows an
  unlisted add-on to omit it, or point it at a real, static JSON file this
  repo controls. Keep the opaque `@...invalid` *id* as-is -- ids are never
  resolved as URLs, only `update_url` is.
- Startup self-check: journal (see 0689) the plugin's own version, install
  path, and whether `strict_max_version` still admits the running Zotero,
  so a future disappearance leaves a trace of its own diagnosis.
- Version-bump discipline: a test asserts each delivered XPI's manifest
  `version` is greater than the last one tagged/delivered.

## Acceptance

- [x] `verify-install` reports present/absent from `extensions.json`,
      checked once while the plugin is known installed (positive control)
      and once after deliberate removal (negative control).
      `make sitter-verify-install SITTER_PROFILE=...`; three arms, not two —
      the third is a foreign add-on carrying our `version`, `active` and
      `location`, which a reader keyed on the wrong field would call present.
      A profile Zotero has never opened reports "could not read" (exit 3),
      never "absent".
- [x] No unresolvable URL remains in the manifest. `update_url` now names
      `bench/sdt-sitter/update.json`, committed here and served raw from
      `main`; the `.invalid` **id** stays, since Zotero never dereferences one.
- [x] The startup self-check emits one `emit('startup', ...)` record — plugin
      version, `rootURI`, `Zotero.version`, `strict_min_version`,
      `strict_max_version` — as the first thing after
      `Zotero.initializationPromise`, so a disappearance leaving no record at
      all is distinguishable from one whose manifest could not be read. It goes
      through 0689's channel rather than a `Zotero.debug()` of its own, which is
      the fold this ticket's Imagine note deferred to 0689 and 0689 did not
      make. Driven in `tests/sdt_sitter_startup.mjs` against a stub whose
      `uiReadyPromise` rejects, which is what proves the record is emitted
      before anything else can fail.
      **Deviation the author should arbitrate.** Work item 3 asks the
      self-check to say "whether `strict_max_version` still admits the running
      Zotero". This logs the raw values and computes no verdict. The reason is
      that the host has already made exactly that comparison — it is what
      decides whether the plugin loads at all — so a second verdict here could
      only ever disagree with the one that mattered, and a range matcher of our
      own is a new thing to be wrong. The raw values let anyone reading the log
      make the comparison. If the author wants the verdict computed, say so and
      it is a small follow-up.
- [x] `bench/check_sitter_version.py` (in `make check`) refuses a payload
      shipped twice under one manifest version, and a version that goes
      backwards. Its own controls are in `tests/test_check_sitter_version.py`:
      a shallow clone and a linked worktree of one, a grafted history, a
      repository with no commit for the payload path, a partial clone whose
      blobs are gone, and a redirected `GIT_DIR` — each reporting NOT-RUN
      rather than green, because a gate that cannot look must not answer as
      though it had.
- [x] Author has answered the discriminating question above, and the
      answer is recorded in this ticket's log before closing it.
      He answered: no Zotero update in between, and installed via Tools ->
      Add-ons -> Install Add-on From File. That rules out **both** mechanisms
      the Context names — the persistent install path was already in use, and
      no version change occurred for an administrative incompatibility to fire
      on. **The cause is open**, and is left open rather than replaced with a
      third guess. What changes is that a repeat leaves evidence: the startup
      line names the build, and `verify-install` separates three states that
      look identical today — recorded but `active: false`, record gone with the
      XPI still in `extensions/`, and the XPI itself gone. Settling it needs a
      live reproduction, which no code here can perform.
