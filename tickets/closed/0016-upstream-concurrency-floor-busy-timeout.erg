%erg 0.1
Title: Upstream concurrency floor: the cross-library wipe guard (busy_timeout overtaken upstream)
Created: 2026-08-26
Author: claude
Closed: PR-3 merged upstream as daf576b in v1.11.0; PR-2 sunset-closed 2026-08-27. Both halves resolved

--- log ---
2026-08-26T15:43Z claude created

2026-08-26T16:53Z claude note PR-2 rescoped to busy_timeout alone per the ratified re-form; the commit-cadence half moved to scoped issue C (0035)

2026-08-26T17:05Z claude note body reconciled with the rescope (code review): per-page-commit wording removed from PR-2's scope; the cadence amendment now points at 0035
2026-08-27T15:50Z claude note PR-2 closed by the sunset rule, overtaken by his own implementation: v1.7.1 (80f8aa0, #18) ships BUSY_TIMEOUT_MS = 10_000 with PRAGMA busy_timeout on every open plus WAL tolerance — more than PR-2 proposed. DECISIONS.md 2026-08-27. PR-3 verified still live at v1.8.0 (309204b): clearStore() still reached from the build path (index-manager.ts:462), no library identity stamped; 2f453d6's router pinning holds the backend still per crawl but stamps nothing
2026-08-28T17:55Z claude note feasibility re-check at v1.9.0 (bb414df, 2026-08-28): PR-3's hazard verified still live — clearStore() still reached from the build path (index-manager.ts:462), no library identity stamped anywhere in src/features/search/, and the delta since 309204b (our #25 as fd51659; annotate feature 87e06c0) touches nothing under src/features/search/. Line drift: the single-library doc-comment to quote now sits at build.ts:205. #25 merged and shipped in v1.9.0, so both in-flight slots are free and the contained-PR budget's live remainder is four. Launchable now
2026-08-28T18:45Z claude note PR-3 BUILT and pushed to the fork as cross-library-guard (61a0e38, one commit atop bb414df/v1.9.0). The stamp: canonical token (user | group:<id>, personal library one token across the users/0 / cloud-id seam) written with the first rows, persisted in the SQLite meta table and the JSON snapshot, reported as status.library. The guard: assertLibrary refuses a build or update for a different library naming both, asserted synchronously in startIndexBuild/startIndexUpdate (fire-and-forget jobs only log rejections) and again in the engine before reset(). Unstamped legacy indexes refuse nothing; first stamped build adopts them. His doc-comment quoted and the guard framed as enforcing it (political review finding 4). Gates on his tree: typecheck, lint, build clean; 754 passed / 7 skipped (his 745 + 9 new in search-library-guard.test.ts). Counter-proof on stock v1.9.0: the tool-path repro shows a group build silently replacing a 6-item personal index (items 6->4, first hit G0, state done, no notice); the guard file fails 9 of 9 there. CHANGELOG entry under Unreleased and a docs paragraph included (the #25 lesson). Pre-filled PR form in RUNBOOK.md (PR B) — ready to open whenever the author spends the slot
2026-08-29T13:16Z claude note PR-3 OPENED upstream as #32 (2026-08-29), on a slot freed by the v1.10.0 sweep. Rebased from bb414df onto b132f2d (v1.10.0) as ae6b043, force-pushed to the fork: five conflicts, all in files v1.10.0 rewrote, additive except the build prologue where v1.10.0's resume branch owns the reset decision and only the stamp survives (placed after the branch, so a resumed build restamps too). HAZARD RE-VERIFIED AND IT GREW A SECOND SHAPE: resumeFrom() gates on fresh, the checkpoint, a non-empty store and the embedder, never on the library, so a build for another library against an INTERRUPTED index skips clearStore() and appends to the rows already there. Measured on stock v1.10.0 — state done, resumedFrom 100, passages 102, itemsTotal 2: one file holding two libraries. offsetStillHolds() cannot catch it (it invalidates the crawl offset only, keeping committed rows either way). One assertLibrary ahead of resumeFrom() covers both shapes; a tenth test covers the resume shape. Counter-proof: all ten fail on stock v1.10.0, all ten pass on the branch. Gates on his tree: typecheck, lint, build clean; 857 passed / 7 skipped
2026-08-31T14:53Z claude note PR-3 MERGED upstream 2026-08-31 as daf576b, shipped in v1.11.0. Merged as a merge commit rather than a squash, so the fork commit ae6b043 sits in upstream main under its own authorship and committer date with no maintainer follow-up commit — the fourth contributed item to land unmodified after #25, #27 and #28. His merge message records that he reproduced BOTH shapes on main before merging: the clearStore() erasure and the resume-append v1.10.0 added. The shipped guard is what the PR argued — the index stamps the canonical library it holds ('user' or 'group:<id>'), a build or update for another refuses up front naming both and the way forward, the personal library is one identity however addressed so the local/cloud seam never trips it (amendment (a)+(b) both honoured), and pre-stamp indexes refuse nothing and are adopted by their first stamped build. Last exit criterion discharged
2026-08-31T14:53Z Claude closed — PR-3 merged upstream as daf576b in v1.11.0; PR-2 sunset-closed 2026-08-27. Both halves resolved
--- body ---
## Context
DESIGN.md §4 PR-3 (originally PR-2 and PR-3), honoring R13 and R12.
PR-2 (busy_timeout=5000 on every connection, alone) is CLOSED by the sunset
rule: v1.7.1's `80f8aa0` ships `busy_timeout = 10 s` on every open with WAL
tolerance — overtaken by his own implementation, no PR to send (DECISIONS.md
2026-08-27). The edf2748 evidence (zero busy_timeout anywhere in src/; the
0 ms "database is locked" repro) stands as the record of why it mattered. The
per-page commit-cadence question remains with scoped issue C (0035).
PR-3 stands, verified live at v1.8.0: the build path still reaches
clearStore() (index-manager.ts:462) with no library identity stamped, so a
build for one library wipes another's index — the R12 hazard DESIGN-DELTA
cites. The PR: a build for a different library than stamped refuses with a
notice naming both, instead of clearStore(). The guard, not the multi-library
feature.

## Exit criteria
- [x] PR-2 resolved without a PR: overtaken by upstream `80f8aa0` (v1.7.1, busy_timeout 10 s) — sunset-closed 2026-08-27
- [x] PR-3: cross-library refusal with both libraries named; clearStore chain as repro (built 2026-08-28: fork branch cross-library-guard at 61a0e38, gates green 754/7 — see log)
- [ ] PR-3 opened upstream; outcome recorded here

## Amendments (implementation review, 2026-08-26)
PR-2: the persist cadence already exists (index-manager.ts:472-473, 200
items/10 s) BUT crawlOptions overrides it to 500 items/60 s on every fulltext
build (build.ts:321) — the longest-held lock on the path that matters. Per the
re-form that override travels with the cadence question to scoped issue C
(0035); PR-2 stays busy_timeout alone, and its repro is scoped to the
zero-timeout throw the pragma fixes (the fulltext hold window is 0035's
evidence, not this PR's).
PR-3 rescoped: no library identity is persisted today, and build.ts:185-197
deliberately avoids keying by routed library id (users/0 locally aliases
users/<id> on cloud). The PR must (a) stamp a canonical {type,id} with the
personal library normalized to one token, then (b) guard on it — and must not
refuse legitimate rebuilds across the local/cloud seam. Quote his doc-comment
in the body and frame the guard as ENFORCING the documented single-library
assumption (political review, finding 4).
