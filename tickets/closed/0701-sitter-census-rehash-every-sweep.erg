%erg 0.1
Title: Stop the sitter re-hashing every attachment on every 30s sweep
Created: 2026-09-06
Author: claude
Closed: autoclosed — PR #389

--- log ---
2026-09-06T00:00Z claude created

2026-09-06T10:11Z sim note blocker 0691 closed — Blocked-by removed.
2026-09-06T10:25Z sim note blocker 0689 closed — Blocked-by removed.
2026-09-06T00:00Z claude note Imagine phase (raid, sonnet): PROCEED WITH CHANGES. Confirmed unchanged against current bootstrap.js/scheduler.js -- inspect() still hashes unconditionally at line 435, sweep wrapper still reschedules on a fixed 30s timer regardless of outcome. Not incidentally fixed by Wave A. Line-level conflict with ticket 0702: both edit the sweep reschedule wrapper in bootstrap.js. Land 0702 first (it turns the wrapper into a try/finally); rebase this ticket's idle-backoff-interval logic onto 0702's already-guarded wrapper. The hash-avoidance half (source fingerprint cache) is independent and can land in the same PR as the interval change.
2026-09-06T12:11Z claude note createSDTSourceHashes memoizes the source MD5 against (path, size, mtime) — in memory, derived, pruned on the census 'seen' set, so the disposable-cache-only ruling is untouched. inspect() also drops one syscall per attachment (the exists() and the stat() asked the same file the same question). Idle poll backs off to 10 min when the previous sweep ended 'waiting' with state.candidates === 0; candidates rather than pending, because pending drains as documents settle. No notifier subscription — the ticket allows a floor poll and the parent scoped it to the interval. Seven control arms verified red, including one that first survived: an erroring census also has candidates 0, and only the phase separates it from a finished library.
2026-09-06T15:39Z claude note Author ruled on the mechanism substitution: 'Add a periodic full re-verify.' Implemented as a 24 h expiry per memoized entry rather than the per-restart option also offered — per-restart was already true (the Map is built in initialize(), so disable/restart discards it) and so bought nothing; the case needing a bound is a session running for weeks. Age outside [0, 24 h) re-verifies, so a backwards NTP step shortens the window rather than extending it. DECISIONS.md carries the ruling verbatim; SPEC.md §5 drops 'live' from 'live source hash' and states the window, the bound and the in-session lifetime, and discloses the 30 s / 10 min census cadence this ticket also introduced.
2026-09-06T16:01Z sim closed — autoclosed — PR #389
--- body ---
## Context

Fable 5.1 deep-QA pass (2026-09-06), verified against the actual installed
Zotero 10.0.1 source (`/opt/zotero7/app/omni.ja`), not assumptions.
`inspect()` (bootstrap.js) calls `await item.attachmentHash` -- a full,
uncached MD5 of the source file -- before its own cache lookup, because
the cache key embeds the hash. `sweep()` re-censuses the whole library on
every invocation, and the bootstrap wrapper reschedules a sweep 30s after
every sweep ends regardless of outcome (waiting, blocked, or error). Net
effect: continuous full-library disk I/O and CPU all night, every night,
directly contradicting the sitter's own "quiet supervisor, minimal
footprint" design ethos. Native Zotero's own SDT code already caches a
fingerprint keyed on (path, size, mtime) specifically to avoid this; the
sitter does not reuse it.

This is a plausible architectural contributor to the author's own
observed hang this morning ("stuck in a census loop") -- see ticket for
the heartbeat-blind-spot half of that story.

## Work

- Cache (path, size, mtime) per attachment identity alongside the existing
  pack-open cache record; skip the MD5 hash entirely when they match the
  last recorded values, matching native's own fingerprint discipline.
- Do not re-census on a fixed timer when the previous sweep ended
  `waiting` with zero candidates -- prefer reacting to a Zotero notifier
  event (new/changed attachment) over blind polling. If a fixed floor
  poll is kept for robustness, make its interval much longer than 30s
  when idle.
- Preserve the disposable-cache-only design (no durable ledger) --
  the fingerprint is derived/reconstructible, not a new persisted claim
  about job state.

## Acceptance

- [x] A census with zero changed attachments performs zero new hash
      reads, verified by counting `attachmentHash`/`IOUtils` calls in a
      mock host across two consecutive sweeps with an unchanged library.
- [x] An idle library (no candidates) does not re-census every 30s.
- [x] Existing scheduler tests still pass unmodified in behavior for a
      library with actual changes.
