%erg 0.1
Title: frac_vec calibration from library-derived pairs
Created: 2026-08-26
Author: claude
Label: deferred

--- log ---
2026-08-26T16:46Z claude created

2026-08-26T16:46Z Claude label deferred

2026-08-26T17:05Z claude note deferral condition repointed (code review): 0023's PR-12 is closed into scoped issue A (0033)
2026-08-31T05:38Z claude note METHOD QUESTION RAISED, awaiting ratification in spec/DECISIONS.md (2026-08-30): spec/FIELD-REVIEW.md instructs this ticket to read Zotero.Embeddings.Calibration at source before committing to its own calibration, on the ground that the procedure is an algorithm with parameters rather than an idea and so the survey's usual read-describe-rebuild route does not carry it. zotero/zotero is AGPL-3.0 and zoteus is MIT, and the chain gives this ticket that instruction with no bound on what the reading may produce. Do not start the calibration work against upstream source until the ledger entry is ruled on; the entry lists four options and recommends none
--- body ---
## Context
DESIGN.md §2.6 defers #6012-style calibration (mean centering, noise floor =
p99 of unrelated pairs, display ceiling = median of matched pairs, reject a
model whose matched-median <= null-p99) "to its own ticket with a stated
pair-generation protocol" — this is that ticket. Protocol: matched pairs =
title<->abstract of one item; unrelated pairs = cross-item; the library is the
corpus. Until built, frac_vec defaults to list-local max-normalization.
Deferred: only worth building if fraction-RRF ships (the query-semantics work
riding scoped issue A, 0033 — formerly 0023's PR-12) and the golden gate shows
the default normalization losing.

## Exit criteria
- [ ] calibration block computed per model-version from library-derived pairs
- [ ] model-rejection rule enforced ("better to fail loudly than index with it")
- [ ] frac_vec uses the calibrated floor/ceiling; golden set shows no regression
