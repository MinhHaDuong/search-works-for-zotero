%erg 0.1
Title: Scoped issue B: entries and the segmenter
Created: 2026-08-26
Author: claude
Blocked-by: 0032

--- log ---
2026-08-26T16:52Z claude created

2026-08-26T17:11Z claude note Copilot round 4: edges mechanized (train, 0028, 0032); the X5 gate stays an exit criterion rather than Blocked-by 0025, which would cycle through X3b
2026-08-28T16:24Z Claude note blocker 0015 closed — Blocked-by removed.
2026-08-29T09:58Z Minh Ha-Duong note blocker 0014 closed — Blocked-by removed.
2026-08-29T14:07Z Minh Ha-Duong note blocker 0017 closed — Blocked-by removed.
2026-08-30T17:04Z claude note #6012 CHECKPOINT READ 2026-08-30, at source (see 0028's entry of the same date for the full reading and the SHAs). VERDICT for this ticket's double gate: the checkpoint half is CLEAR -- the segmenter half of this issue is not obsolete and the data-model half is not reduced to an adapter question, because structured extraction does not reach the local API (sdt.js ships on main; server_localAPI.js has zero sdt/structured references; the only text surface is flat /fulltext). #6012 is still draft, unmerged, unmoved since 2026-08-29. The X5 half of the gate remains OPEN and is the binding one: X5 is unstarted, blocked on 0028. One finding that sharpens CONSTRAINTS C2's last bullet from prediction to verified mechanism: the PR adds a 'bestMatch' search CONDITION (searchConditions.js), and main's local API already serves /searches/:searchKey/items -- so on merge, a saved search carrying that condition becomes the first place platform semantic results reach the local API, with no new endpoint required. Nothing to re-draft here yet; do not treat the cleared half as permission to draft while X5 is open
2026-08-31T14:53Z Claude note blocker 0016 closed — Blocked-by removed.
2026-09-03T15:32Z MinhHaDuong note blocker 0028 closed — Blocked-by removed.
--- body ---
## Context
GOVERNANCE.md's increment train, item 6 (scoped issue B), per the ratified re-form (DECISIONS.md 2026-08-26).
One #10-shaped issue for the entry unit: the entries schema + slab references
(DESIGN §2.2), the segmenter (measured by X5, built by 0028), the
per-attachment data model + first-with-text + stored skip reasons (absorbed
from closed ticket 0018 — the verified concatenation defect at
fulltext-source.ts:119-146 is the reproduction), and the two decisions the
implementation review says the text must carry: the memory backend's fate
under a per-field schema, and multi-tenant suffixing of the v2 filename.
DOUBLE-GATED: X5's verdict (0025) AND the #6012 checkpoint — structured
extraction landing upstream makes this issue's segmenter half obsolete and
its data-model half an adapter question; check before drafting, not after.

## Exit criteria
- [x] #6012 checkpoint recorded here 2026-08-30 — read at source; the checkpoint half of the double gate is clear, X5 still open
- [ ] X5 verdict in hand before any segmenter number is quoted
- [ ] issue drafted and filed per DESIGN §4 ordering; response recorded here
