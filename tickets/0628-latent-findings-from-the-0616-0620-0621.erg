%erg 0.1
Title: Latent findings from the 0616/0620/0621/0624/0626 raid wave
Created: 2026-09-03
Author: MinhHaDuong

--- log ---
2026-09-03T15:50Z MinhHaDuong created
2026-09-03T15:58Z claude note roar sweep for the class 0626 fixed (missing spawn-witness in a posture-refusal test) found it live in the two adapters cited as the pattern to mirror. Finding 7 added
2026-09-03T16:05Z claude note findings 3 and 4 fixed by 0621's own execute agent (PR #312, merged): exact rel_tol equality plus a rank cap of 10x the limit replaces the unbounded six-place-rounded inversion, and a math.isfinite guard replaces the NaN-crashing round(). All three artifacts reproduce unchanged; tests 13 -> 23

--- body ---
## Context

Merged 2026-09-03 without waiting on these: none of them affect any current
verdict or committed artifact, and each requires a condition this codebase
doesn't currently produce. Bundled here rather than filed as five tickets,
per the severity floor — none alone clears the bar for a dedicated ticket.

## Findings

1. **0620 (`tests/test_index_schema_fixtures.py`)** — the module-level
   `pytestmark` applies `skipif(node missing)` to every test in the file,
   including the new leg-3 tests, which only need `git`. On a node-less
   machine holding a valid `upstream.git/` mirror, the new leg silently
   SKIPs instead of reporting NOT-RUN — the exact "skip and pass are the
   same colour" defect this ticket exists to close, one layer up. `make
   schema-gate` (added by the same PR) sidesteps this for its own strict
   run, but the plain suite doesn't. Fix: exempt the leg-3 tests from the
   node skipif, or split them into their own module.

2. **0620 (same file)** — `check_declaration_against_upstream`'s regex
   takes the *first* `^const SCHEMA_VERSION` match in the upstream file.
   An unprefixed historical value inside a `/* */` block comment (sitting
   at column 0) ahead of the live declaration would be read as the answer.
   Not live today (today's upstream file has no such comment), cheap fix:
   assert exactly one match, or take the last one.

3. **FIXED (PR #312).** 0621 (`bench/smoke_upstream.py`) — `_rrf_rank_of` had
   no upper bound on the recovered rank. Below roughly score < 0.0164,
   6-decimal rounding became coarser than the spacing between consecutive
   RRF values, so an increasing fraction of ordinary weak similarity scores
   got accepted as "proven RRF rank," defeating the diagnostic's purpose for
   that range. Informational field only — never moved any pass/fail, and no
   committed artifact had a score in that range. Fixed with exact equality
   (`rel_tol=1e-9`) and a rank cap of 10x the limit.

4. **FIXED (PR #312).** 0621 (same file) — a NaN score crashed the whole
   script (uncaught `ValueError` in `round(1/float(score))`), losing the
   JSON artifact for every check, not just this one. Fixed with a
   `math.isfinite` guard before the round(), covering NaN and both
   infinities.

5. **0624 (`bench/acceptance/adapters/zoteus.py`)** — the README row and
   two code comments claim the work-counter derivation covers "any build
   that starts reporting counters... with no edit to the adapter." The
   actual mechanism only recognizes counters under the literal top-level
   keys `work`/`counters` in two specific RPC replies (`WORK_OBJECT_KEYS`).
   A build that groups counters under a third key name, or nests one level
   deeper, is silently read as `None` — the same overclaim shape this
   ticket was filed to close, relocated into the fix's own wording. Fix:
   narrow the three claims to name the actual key set and replies, or
   widen `_work_counters` to match what's claimed.

6. **0626 (`tests/test_acceptance_adapter_{zotseek,zotero_core_6012,zoteus}.py`)**
   — none of the three new posture-refusal tests check that `wrap()`'s
   *return value* is actually used. If a future edit dropped `argv =
   self._posture.wrap(...)` to a bare `self._posture.wrap(...)`, all three
   refusal tests would stay green (refusal raises unconditionally before
   returning anything), but a real *accepted* posture would silently spawn
   the target unwrapped — the identity-boundary breach tickets 0625/0626
   exist to prevent. Confirmed empirically. Pre-existing gap across all
   five adapters (including beaver/zotero_mcp), not introduced by 0626;
   out of that ticket's stated scope (mirrors an existing refusal-only
   pattern) but worth closing once, for all five adapters together.

7. **0625/0626 (`tests/test_acceptance_adapter_{beaver,zotero_mcp}.py`)** —
   found by sweeping for the exact class 0626 fixed in `zotero_core_6012`
   (a refusal test that stays green even if `wrap()` moves to just after
   the spawn, because `PostureUnavailable` still raises, one line late,
   with the process/server already constructed). Neither of the two
   *already-covered* adapters — the ones 0626's new tests were written to
   mirror — has the trailing witness assertion (`self._process is None` /
   `self.server is None`) that makes that mutation detectable:
   - `zotero_mcp.py`: **live gap, not just theoretical.** Its `running()`
     calls `drive.Server(cmd, env, self.timeout)` right after `wrap()`
     (`zotero_mcp.py:417-419`), and `mcp_drive.Server.__init__` spawns a
     real subprocess on construction on any machine with `node` installed
     — the same shared helper that made the identical mutation catchable
     only by a witness assertion in `zoteus.py`. A future edit moving
     `wrap()` after `drive.Server(...)` would leave this test green while
     spawning the target unwrapped.
   - `beaver.py`: same missing assertion, but currently caught by
     fixture coincidence instead — `zotero` points at a nonexistent
     binary, so a late-wrap mutation gets a `FileNotFoundError` before
     `PostureUnavailable`, same shape as `zotseek.py`'s test (which 0626
     itself flagged as resting on coincidence rather than the assertion).
   Fix: add `assert self._server is None` (`zotero_mcp.py`) and
   `assert self._process is None` (`beaver.py`) after each `pytest.raises`
   block, mirroring `zoteus.py`'s and `zotero_core_6012.py`'s tests.

## Exit criteria

- [ ] Each finding above is fixed, or explicitly deferred again with a
      reason, one at a time or in whatever grouping makes sense
      (findings 3, 4 fixed — PR #312; findings 1, 2, 5, 6, 7 remain)
