%erg 0.1
Title: Standing guard: every bench driver that opens a real index is exercised against a current-schema fixture
Created: 2026-08-29
Author: claude

--- log ---
2026-08-29T10:15Z claude created
2026-08-29T10:15Z claude note filed separately from 0100 on purpose: 0100 fixes two instances, this covers the class. Bundling the guard into the fix would make it a per-PR check, where what is needed is something that fires when an UNRELATED future change breaks the same assumption

2026-08-31T14:57Z claude note gains Blocked-by 0504. Action 4 records in UPSTREAM which schema generation the drivers target, and UPSTREAM cannot name a generation before the baseline it belongs to is reviewed. The need got sharper the same day: upstream v1.11.0 (#34) shipped a schema ladder and vector salvage, so SCHEMA_VERSION is no longer a constant that has never moved -- which is exactly the change this ticket was filed in anticipation of
2026-08-31T15:26Z claude note Blocked-by 0504 REMOVED, and the schema-generation criterion is recommended for deletion -- both on the same evidence, which contradicts this morning's note. SCHEMA_VERSION has been 1 since eee1000 (v1.7.0) and is STILL 1 at v1.12.0, and the passages DDL is byte-identical between v1.10.0 and main: source TEXT already existed, which is why own-words needed no schema change. So v1.11.0's ladder is machinery built BEFORE the first bump, not a bump. Recording the generation in UPSTREAM therefore records a constant that has never moved, guarding a break that has never happened, and the ladder makes that break cheaper when it comes rather than more likely. The criterion is left in place rather than deleted unilaterally -- it is not this session's to cut -- but it should go at the next touch. This ticket's real content, drivers exercised against a current-schema fixture, is untouched by any of it and still stands
2026-09-02T19:16Z claude note unblocked by 0100's merge, and its scope is wider than 0100 assumed: vec_real_measure.mjs, vec_mrl_recall.mjs and issue30_build_index.mjs also open a real index against the pre-rename schema, so the fixture suite covers five drivers. bench/index_schema.mjs (landed with 0100) gives the guard its assertion and its loud-failure message; what 0101 still owes is the two committed fixtures and the standing test
2026-09-02T19:18Z Minh Ha-Duong note blocker 0100 closed — Blocked-by removed.
2026-09-02T20:24Z the class is eight drivers, not five, and three of them cannot be migrated: vec_real_measure/vec_mrl_recall/issue30_build_index target the PRE-RENAME generation on purpose — their substrate is sqlite-vec passage_vectors, which the current schema has no equivalent of (grep of fork/src at the reviewed SHA: passage_vectors, passage_meta, index_meta all absent). Migrating them would replace a measurement, not repair a driver, and would invalidate the 0008 artifacts. So each driver DECLARES its generation and assertPreRenameSchema mirrors assertIndexSchema; year_scope, query_arms, query_arms_multi gained the current-schema gate
2026-09-02T20:24Z criterion 4 implemented rather than deleted, per the ruling this session: UPSTREAM_INDEX_SCHEMA_VERSION=1 is written with teeth, not as decoration. The fixture generator stamps meta.schemaVersion from the mirror in index_schema.mjs, and the test asserts declaration == stamp, plus a third leg reading upstream's own const SCHEMA_VERSION when a fork checkout is present (verified positive against fork/src: 'const SCHEMA_VERSION = 1'). The 2026-08-31 argument for deletion stands on its own terms — the constant has never moved — but the generator gives it a consumer it did not have then
2026-09-02T20:24Z seen red before it was seen green: with the fixtures present and the gates absent, 6 of the 8 refusal cases failed — year_scope died on 'no such table: items', query_arms on 'no such table: meta', and query_arms_multi / issue30 / both vec drivers never reached the index at all (missing dist, missing sqlite-vec), so none of the six named a schema generation. The two 0100-repaired drivers were already green in both directions, which is what says the suite discriminates rather than failing everything
--- body ---
## Context
The /roar sweep after X2 found two bench drivers pinned to the pre-rename
index schema (0100). Two instances of one assumption is a class, and the
class has a nasty shape: upstream owns the schema, this repo owns the
drivers, and nothing connects them. The rename happened upstream, every
driver here kept passing `ruff` and `pytest`, and the breakage surfaced only
when someone tried to run one — an hour into a measurement session, against
a deadline.

It will happen again. Upstream renames tables when it wants to, and this repo
has no notification, no version pin on the schema, and no test that opens an
index at all. `make check` covers the harness's own Python and the figure
guard; it never touches SQLite.

The guard must therefore fire on a change nobody thought to connect to
`bench/` — which is why it is a standing test rather than a line in 0100's
diff.

## Relevant files
- `tests/` — currently `test_check_figures.py` and `test_query_latency.py`,
  both pure-Python; nothing here opens a database
- `bench/index_concentration.mjs`, `bench/bm25_idf_effect.mjs`,
  `bench/constrained_match.mjs`, `bench/query.py` — the drivers that take a
  real index path
- `UPSTREAM` — carries the reviewed SHA, and is the natural place to record
  the schema generation the drivers expect

## Actions
1. Build a TINY current-schema fixture — a few passages, both tables,
   committed or generated by a script in seconds — not the 949 MB real index.
   The fixture is the positive control: without one, a guard that finds
   nothing is indistinguishable from a guard that could not look.
2. Add a test, `integration`-marked, that runs each real-index driver against
   the fixture and asserts it produces its artifact shape.
3. Add the negative case: an old-schema fixture, against which every driver
   must FAIL LOUDLY and name the schema it found. This is the case that would
   have gone red before 0100 and is the one that earns the suite's place.
4. Record in `UPSTREAM` which schema generation the drivers target, so a
   future upstream rename has somewhere to land.

## Test
The negative fixture IS the test — it must fail against the pre-0100 drivers.
Verify by checking out a driver at its current SHA and confirming the new
test goes red; a guard never seen to fail is not a guard.

## Verification
- [x] the old-schema fixture makes the pre-0100 drivers go red — and six more
      besides: with the fixtures present and the gates not yet written, six of
      the eight refusal cases failed, none of them naming a schema generation
- [x] the current-schema fixture makes the repaired drivers go green, and two of
      the eight run end to end on it and emit their artifact
- [x] fixtures are small enough that `make check` stays under its budget: 0,2 s
      to generate, ~250 KB each, and the whole new module runs in 2,7 s
- [x] `UPSTREAM` names the expected schema generation, and the test gives the
      declaration teeth rather than leaving it decorative

## Invariants
- The fast tier stays pure Python: these tests are `integration`, per the
  harness's cost-based tiering.
- No test depends on the author's 949 MB index or on a live Zotero.

## Exit criteria
- A standing test opens each real-index driver against both fixtures, the
  negative case has been SEEN to fail against the old drivers, and `make
  check` is green.
