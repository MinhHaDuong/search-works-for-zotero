# Machine-readable upstream review baseline. Sourced by Makefile.
UPSTREAM_REPOSITORY=https://github.com/oscardvs/zoteus.git
FORK_REPOSITORY=https://github.com/MinhHaDuong/zoteus.git
UPSTREAM_BRANCH=main
# The reviewed SHA is the v1.20.2 tag itself, which is also `main`'s tip on the
# day of the read (2026-09-17). Six releases past v1.16.0 (v1.17.0 to v1.20.2),
# 73 commits, 53 watched files +5406/-611: a keyed attachment map with bounded
# recovery and a `fulltextPartial` status flag (#78, #67), a pending-cloud-write
# overlay on the library router with four newly routed reads (#64, #74, #80, #81),
# stock exports through the router (#75), `outputSchema` on every tool (v1.20.0),
# the `zotero_pdf_images` tool, a single pdfjs import site (`pdfjs-loader.ts`,
# the #62 failure class), `pdfjs-dist` pinned exactly (#69), `writeMeta` merging
# and `refreshFromStore` so two processes stop clobbering each other's stamp
# (#68), and the semantic-mode refusal from our PR #73. Not a documentation
# release. `verification/UPSTREAM-1.20.2-REVIEW.md` is the delta review and
# `verification/UPSTREAM-1.20.2-REREAD.md` the row re-read.
UPSTREAM_REVIEWED_SHA=c386e835a2ef90b539b611168130c8540cd55fe5
UPSTREAM_REVIEWED_VERSION=v1.20.2
UPSTREAM_REVIEWED_DATE=2026-09-16
# The index schema generation the bench drivers target: upstream's `SCHEMA_VERSION` in
# `src/features/search/sqlite-index.ts`, mirrored in `bench/index_schema.mjs` and stamped
# into the current-generation fixture by `bench/fixtures/make_index_fixture.mjs`. It was 1
# from v1.7.0 to v1.12.0 and moved to 2 in v1.13.0, when keeping diacritics in the keyword
# index changed the FTS tokenizer and added the `accent_variants` table. That bump is the
# first this line has ever recorded, and it arrived while the leg of
# `tests/test_index_schema_fixtures.py` that compares this declaration against upstream's
# own constant was SKIPPING for want of a `fork/` checkout — see ticket 0620.
UPSTREAM_INDEX_SCHEMA_VERSION=2
